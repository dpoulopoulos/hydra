// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { accountsCreateAccount, accountsDeleteAccount, accountsGetAccount, accountsListAccounts, accountsUpdateAccount, budgetsBulkUpsertBudgets, budgetsCopyBudgets, budgetsCreateBudget, budgetsDeleteBudget, budgetsGetBudget, budgetsListBudgets, budgetsUpdateBudget, categoriesCreateCategory, categoriesDeleteCategory, categoriesGetCategory, categoriesGetCategoryTree, categoriesListCategories, categoriesUpdateCategory, emailVerificationResendVerificationEmail, emailVerificationVerifyEmail, householdsAcceptHouseholdInvite, householdsCreateHouseholdInvite, householdsGetHouseholdMe, householdsLeaveHousehold, householdsListHouseholdInvites, householdsListHouseholdMembers, householdsPreviewHouseholdInvite, householdsRemoveHouseholdMember, householdsRevokeHouseholdInvite, householdsUpdateHouseholdMe, householdsUpdateHouseholdMember, loginLoginAccessToken, type Options, passwordResetConfirmPasswordReset, passwordResetRequestPasswordReset, passwordResetVerifyPasswordResetToken, recurringRulesCreateRecurringRule, recurringRulesDeleteRecurringRule, recurringRulesGetRecurringRule, recurringRulesListRecurringRules, recurringRulesListUpcomingOccurrences, recurringRulesRunRecurringRules, recurringRulesUpdateRecurringRule, reportsBudgetProgress, reportsIncomeExpense, reportsMonthSummary, reportsSpendByCategory, reportsSpendOverTime, transactionsCreateTransaction, transactionsDeleteTransaction, transactionsGetTransaction, transactionsListTransactions, transactionsUpdateTransaction, usersCreateUser, usersDeleteUser, usersDeleteUserMe, usersGetUserById, usersGetUserMe, usersGetUsers, usersRegisterUser, usersUpdatePasswordMe, usersUpdateUser, usersUpdateUserMe } from '../sdk.gen';
import type { AccountsCreateAccountData, AccountsCreateAccountError, AccountsCreateAccountResponse, AccountsDeleteAccountData, AccountsDeleteAccountError, AccountsDeleteAccountResponse, AccountsGetAccountData, AccountsGetAccountError, AccountsGetAccountResponse, AccountsListAccountsData, AccountsListAccountsError, AccountsListAccountsResponse, AccountsUpdateAccountData, AccountsUpdateAccountError, AccountsUpdateAccountResponse, BudgetsBulkUpsertBudgetsData, BudgetsBulkUpsertBudgetsError, BudgetsBulkUpsertBudgetsResponse, BudgetsCopyBudgetsData, BudgetsCopyBudgetsError, BudgetsCopyBudgetsResponse, BudgetsCreateBudgetData, BudgetsCreateBudgetError, BudgetsCreateBudgetResponse, BudgetsDeleteBudgetData, BudgetsDeleteBudgetError, BudgetsDeleteBudgetResponse, BudgetsGetBudgetData, BudgetsGetBudgetError, BudgetsGetBudgetResponse, BudgetsListBudgetsData, BudgetsListBudgetsError, BudgetsListBudgetsResponse, BudgetsUpdateBudgetData, BudgetsUpdateBudgetError, BudgetsUpdateBudgetResponse, CategoriesCreateCategoryData, CategoriesCreateCategoryError, CategoriesCreateCategoryResponse, CategoriesDeleteCategoryData, CategoriesDeleteCategoryError, CategoriesDeleteCategoryResponse, CategoriesGetCategoryData, CategoriesGetCategoryError, CategoriesGetCategoryResponse, CategoriesGetCategoryTreeData, CategoriesGetCategoryTreeError, CategoriesGetCategoryTreeResponse, CategoriesListCategoriesData, CategoriesListCategoriesError, CategoriesListCategoriesResponse, CategoriesUpdateCategoryData, CategoriesUpdateCategoryError, CategoriesUpdateCategoryResponse, EmailVerificationResendVerificationEmailData, EmailVerificationResendVerificationEmailError, EmailVerificationResendVerificationEmailResponse, EmailVerificationVerifyEmailData, EmailVerificationVerifyEmailError, EmailVerificationVerifyEmailResponse, HouseholdsAcceptHouseholdInviteData, HouseholdsAcceptHouseholdInviteError, HouseholdsAcceptHouseholdInviteResponse, HouseholdsCreateHouseholdInviteData, HouseholdsCreateHouseholdInviteError, HouseholdsCreateHouseholdInviteResponse, HouseholdsGetHouseholdMeData, HouseholdsGetHouseholdMeResponse, HouseholdsLeaveHouseholdData, HouseholdsLeaveHouseholdResponse, HouseholdsListHouseholdInvitesData, HouseholdsListHouseholdInvitesError, HouseholdsListHouseholdInvitesResponse, HouseholdsListHouseholdMembersData, HouseholdsListHouseholdMembersResponse, HouseholdsPreviewHouseholdInviteData, HouseholdsPreviewHouseholdInviteError, HouseholdsPreviewHouseholdInviteResponse, HouseholdsRemoveHouseholdMemberData, HouseholdsRemoveHouseholdMemberError, HouseholdsRemoveHouseholdMemberResponse, HouseholdsRevokeHouseholdInviteData, HouseholdsRevokeHouseholdInviteError, HouseholdsRevokeHouseholdInviteResponse, HouseholdsUpdateHouseholdMeData, HouseholdsUpdateHouseholdMeError, HouseholdsUpdateHouseholdMemberData, HouseholdsUpdateHouseholdMemberError, HouseholdsUpdateHouseholdMemberResponse, HouseholdsUpdateHouseholdMeResponse, LoginLoginAccessTokenData, LoginLoginAccessTokenError, LoginLoginAccessTokenResponse, PasswordResetConfirmPasswordResetData, PasswordResetConfirmPasswordResetError, PasswordResetConfirmPasswordResetResponse, PasswordResetRequestPasswordResetData, PasswordResetRequestPasswordResetError, PasswordResetRequestPasswordResetResponse, PasswordResetVerifyPasswordResetTokenData, PasswordResetVerifyPasswordResetTokenError, PasswordResetVerifyPasswordResetTokenResponse, RecurringRulesCreateRecurringRuleData, RecurringRulesCreateRecurringRuleError, RecurringRulesCreateRecurringRuleResponse, RecurringRulesDeleteRecurringRuleData, RecurringRulesDeleteRecurringRuleError, RecurringRulesDeleteRecurringRuleResponse, RecurringRulesGetRecurringRuleData, RecurringRulesGetRecurringRuleError, RecurringRulesGetRecurringRuleResponse, RecurringRulesListRecurringRulesData, RecurringRulesListRecurringRulesError, RecurringRulesListRecurringRulesResponse, RecurringRulesListUpcomingOccurrencesData, RecurringRulesListUpcomingOccurrencesError, RecurringRulesListUpcomingOccurrencesResponse, RecurringRulesRunRecurringRulesData, RecurringRulesRunRecurringRulesError, RecurringRulesRunRecurringRulesResponse, RecurringRulesUpdateRecurringRuleData, RecurringRulesUpdateRecurringRuleError, RecurringRulesUpdateRecurringRuleResponse, ReportsBudgetProgressData, ReportsBudgetProgressError, ReportsBudgetProgressResponse, ReportsIncomeExpenseData, ReportsIncomeExpenseError, ReportsIncomeExpenseResponse, ReportsMonthSummaryData, ReportsMonthSummaryError, ReportsMonthSummaryResponse, ReportsSpendByCategoryData, ReportsSpendByCategoryError, ReportsSpendByCategoryResponse, ReportsSpendOverTimeData, ReportsSpendOverTimeError, ReportsSpendOverTimeResponse, TransactionsCreateTransactionData, TransactionsCreateTransactionError, TransactionsCreateTransactionResponse, TransactionsDeleteTransactionData, TransactionsDeleteTransactionError, TransactionsDeleteTransactionResponse, TransactionsGetTransactionData, TransactionsGetTransactionError, TransactionsGetTransactionResponse, TransactionsListTransactionsData, TransactionsListTransactionsError, TransactionsListTransactionsResponse, TransactionsUpdateTransactionData, TransactionsUpdateTransactionError, TransactionsUpdateTransactionResponse, UsersCreateUserData, UsersCreateUserError, UsersCreateUserResponse, UsersDeleteUserData, UsersDeleteUserError, UsersDeleteUserMeData, UsersDeleteUserMeResponse, UsersDeleteUserResponse, UsersGetUserByIdData, UsersGetUserByIdError, UsersGetUserByIdResponse, UsersGetUserMeData, UsersGetUserMeResponse, UsersGetUsersData, UsersGetUsersError, UsersGetUsersResponse, UsersRegisterUserData, UsersRegisterUserError, UsersRegisterUserResponse, UsersUpdatePasswordMeData, UsersUpdatePasswordMeError, UsersUpdatePasswordMeResponse, UsersUpdateUserData, UsersUpdateUserError, UsersUpdateUserMeData, UsersUpdateUserMeError, UsersUpdateUserMeResponse, UsersUpdateUserResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const usersGetUsersQueryKey = (options?: Options<UsersGetUsersData>) => createQueryKey('usersGetUsers', options);

/**
 * Get Users
 *
 * Get registered users.
 *
 * Args:
 * user_service: The user service dependency.
 * skip (int): The number of users to skip.
 * limit (int): The maximum number of users to return.
 *
 * Returns:
 * A list of users.
 *
 * Raises:
 * HTTPException: If the user is not authorized (403), has an invalid token (401), or is not found (404).
 */
export const usersGetUsersOptions = (options?: Options<UsersGetUsersData>) => queryOptions<UsersGetUsersResponse, UsersGetUsersError, UsersGetUsersResponse, ReturnType<typeof usersGetUsersQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await usersGetUsers({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: usersGetUsersQueryKey(options)
});

/**
 * Create User
 *
 * Create a new user.
 *
 * Args:
 * user_service: The user service dependency.
 * household_service: The household service dependency, used to provision
 * the user's household in the same transaction.
 * category_service: The category service dependency, used to seed the
 * household's default categories.
 * user_in: The user creation payload.
 *
 * Returns:
 * The newly created user.
 *
 * Raises:
 * HTTPException: If a user with this email already exists (409),
 * the user is not authorized to perform this action (403),
 * the user's token is invalid (401), or the authenticated
 * user is not found in the database (404).
 */
export const usersCreateUserMutation = (options?: Partial<Options<UsersCreateUserData>>): UseMutationOptions<UsersCreateUserResponse, UsersCreateUserError, Options<UsersCreateUserData>> => {
    const mutationOptions: UseMutationOptions<UsersCreateUserResponse, UsersCreateUserError, Options<UsersCreateUserData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersCreateUser({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Register User
 *
 * Register a new user.
 *
 * This endpoint allows users to register without being authenticated.
 * The user will be created with is_active=False and must verify their email
 * before they can log in.
 *
 * Args:
 * user_service: The user service dependency.
 * email_verification_service: The email verification service dependency.
 * household_service: The household service dependency, used to provision
 * the user's household in the same transaction.
 * category_service: The category service dependency, used to seed the
 * household's default categories.
 * user_in: The user registration data.
 *
 * Returns:
 * The newly created user.
 *
 * Raises:
 * HTTPException: If a user with the same email already exists (409).
 */
export const usersRegisterUserMutation = (options?: Partial<Options<UsersRegisterUserData>>): UseMutationOptions<UsersRegisterUserResponse, UsersRegisterUserError, Options<UsersRegisterUserData>> => {
    const mutationOptions: UseMutationOptions<UsersRegisterUserResponse, UsersRegisterUserError, Options<UsersRegisterUserData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersRegisterUser({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete User Me
 *
 * Delete the current user.
 *
 * Args:
 * user_service: The user service dependency.
 * current_user: The current authenticated user.
 *
 * Returns:
 * A message indicating that the user was deleted successfully.
 *
 * Raises:
 * HTTPException: If a superuser tries to delete their own account (400), the token is invalid (401),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersDeleteUserMeMutation = (options?: Partial<Options<UsersDeleteUserMeData>>): UseMutationOptions<UsersDeleteUserMeResponse, DefaultError, Options<UsersDeleteUserMeData>> => {
    const mutationOptions: UseMutationOptions<UsersDeleteUserMeResponse, DefaultError, Options<UsersDeleteUserMeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersDeleteUserMe({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const usersGetUserMeQueryKey = (options?: Options<UsersGetUserMeData>) => createQueryKey('usersGetUserMe', options);

/**
 * Get User Me
 *
 * Get the current user's information.
 *
 * Args:
 * current_user: The current authenticated user.
 *
 * Returns:
 * The current user's public information.
 *
 * Raises:
 * HTTPException: If the user's token is invalid (401), the user is not found (404), or the user is inactive (403).
 */
export const usersGetUserMeOptions = (options?: Options<UsersGetUserMeData>) => queryOptions<UsersGetUserMeResponse, DefaultError, UsersGetUserMeResponse, ReturnType<typeof usersGetUserMeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await usersGetUserMe({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: usersGetUserMeQueryKey(options)
});

/**
 * Update User Me
 *
 * Update the current user's information.
 *
 * Args:
 * user_service: The user service dependency.
 * user_in: The user data to update.
 * current_user: The current authenticated user.
 *
 * Returns:
 * The updated user information.
 *
 * Raises:
 * HTTPException: If a user with the same email already exists (409), the user's token is invalid (401),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersUpdateUserMeMutation = (options?: Partial<Options<UsersUpdateUserMeData>>): UseMutationOptions<UsersUpdateUserMeResponse, UsersUpdateUserMeError, Options<UsersUpdateUserMeData>> => {
    const mutationOptions: UseMutationOptions<UsersUpdateUserMeResponse, UsersUpdateUserMeError, Options<UsersUpdateUserMeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersUpdateUserMe({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete User
 *
 * Delete a user.
 *
 * This endpoint is only accessible to superusers.
 *
 * Args:
 * user_service: The user service dependency.
 * user_id: The ID of the user to delete.
 *
 * Returns:
 * A message indicating that the user was deleted successfully.
 *
 * Raises:
 * HTTPException: If the user to delete is not found (404), trying to delete any superuser account (400),
 * the current user is not authorized (403), or the token is invalid (401).
 */
export const usersDeleteUserMutation = (options?: Partial<Options<UsersDeleteUserData>>): UseMutationOptions<UsersDeleteUserResponse, UsersDeleteUserError, Options<UsersDeleteUserData>> => {
    const mutationOptions: UseMutationOptions<UsersDeleteUserResponse, UsersDeleteUserError, Options<UsersDeleteUserData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersDeleteUser({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const usersGetUserByIdQueryKey = (options: Options<UsersGetUserByIdData>) => createQueryKey('usersGetUserById', options);

/**
 * Get User By Id
 *
 * Get a specific user by their ID.
 *
 * A user can retrieve their own information. Superusers can retrieve any
 * user's information.
 *
 * Args:
 * user_service: The user service dependency.
 * current_user: The current authenticated user.
 * user_id: The ID of the user to retrieve.
 *
 * Returns:
 * The user's public information.
 *
 * Raises:
 * HTTPException: If the user does not have sufficient privileges (403), the user is not found (404),
 * or the token is invalid (401).
 */
export const usersGetUserByIdOptions = (options: Options<UsersGetUserByIdData>) => queryOptions<UsersGetUserByIdResponse, UsersGetUserByIdError, UsersGetUserByIdResponse, ReturnType<typeof usersGetUserByIdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await usersGetUserById({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: usersGetUserByIdQueryKey(options)
});

/**
 * Update User
 *
 * Update a user's information.
 *
 * This endpoint is only accessible to superusers.
 *
 * Args:
 * user_service: The user service dependency.
 * user_id: The ID of the user to update.
 * user_in: The user data to update.
 *
 * Returns:
 * The updated user information.
 *
 * Raises:
 * HTTPException: If the user is not found (404), the new email already exists (409),
 * the current user is not authorized (403), or the token is invalid (401).
 */
export const usersUpdateUserMutation = (options?: Partial<Options<UsersUpdateUserData>>): UseMutationOptions<UsersUpdateUserResponse, UsersUpdateUserError, Options<UsersUpdateUserData>> => {
    const mutationOptions: UseMutationOptions<UsersUpdateUserResponse, UsersUpdateUserError, Options<UsersUpdateUserData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersUpdateUser({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Update Password Me
 *
 * Update the current user's password.
 *
 * Args:
 * user_service: The user service dependency.
 * password_in: The current and new password.
 * current_user: The current authenticated user.
 *
 * Returns:
 * A message indicating that the password was updated successfully.
 *
 * Raises:
 * HTTPException: If the current password is incorrect (401),
 * the new password is the same as the current one (400),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersUpdatePasswordMeMutation = (options?: Partial<Options<UsersUpdatePasswordMeData>>): UseMutationOptions<UsersUpdatePasswordMeResponse, UsersUpdatePasswordMeError, Options<UsersUpdatePasswordMeData>> => {
    const mutationOptions: UseMutationOptions<UsersUpdatePasswordMeResponse, UsersUpdatePasswordMeError, Options<UsersUpdatePasswordMeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await usersUpdatePasswordMe({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const householdsGetHouseholdMeQueryKey = (options?: Options<HouseholdsGetHouseholdMeData>) => createQueryKey('householdsGetHouseholdMe', options);

/**
 * Get Household Me
 *
 * Get the household of the current user.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 *
 * Returns:
 * The household, with its member count.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsGetHouseholdMeOptions = (options?: Options<HouseholdsGetHouseholdMeData>) => queryOptions<HouseholdsGetHouseholdMeResponse, DefaultError, HouseholdsGetHouseholdMeResponse, ReturnType<typeof householdsGetHouseholdMeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await householdsGetHouseholdMe({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: householdsGetHouseholdMeQueryKey(options)
});

/**
 * Update Household Me
 *
 * Rename the household of the current user.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * household_in: The fields to update.
 *
 * Returns:
 * The updated household.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), or the
 * household no longer exists (404).
 */
export const householdsUpdateHouseholdMeMutation = (options?: Partial<Options<HouseholdsUpdateHouseholdMeData>>): UseMutationOptions<HouseholdsUpdateHouseholdMeResponse, HouseholdsUpdateHouseholdMeError, Options<HouseholdsUpdateHouseholdMeData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsUpdateHouseholdMeResponse, HouseholdsUpdateHouseholdMeError, Options<HouseholdsUpdateHouseholdMeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsUpdateHouseholdMe({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const householdsListHouseholdMembersQueryKey = (options?: Options<HouseholdsListHouseholdMembersData>) => createQueryKey('householdsListHouseholdMembers', options);

/**
 * List Household Members
 *
 * List the members of the household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 *
 * Returns:
 * The members of the household.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsListHouseholdMembersOptions = (options?: Options<HouseholdsListHouseholdMembersData>) => queryOptions<HouseholdsListHouseholdMembersResponse, DefaultError, HouseholdsListHouseholdMembersResponse, ReturnType<typeof householdsListHouseholdMembersQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await householdsListHouseholdMembers({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: householdsListHouseholdMembersQueryKey(options)
});

/**
 * Leave Household
 *
 * Leave the household.
 *
 * The caller keeps their account and is given a fresh, empty household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the caller is the household's only owner (400), or the
 * membership no longer exists (404).
 */
export const householdsLeaveHouseholdMutation = (options?: Partial<Options<HouseholdsLeaveHouseholdData>>): UseMutationOptions<HouseholdsLeaveHouseholdResponse, DefaultError, Options<HouseholdsLeaveHouseholdData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsLeaveHouseholdResponse, DefaultError, Options<HouseholdsLeaveHouseholdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsLeaveHousehold({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove Household Member
 *
 * Remove a member from the household.
 *
 * The removed member keeps their account and is given a fresh, empty household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * user_id: The ID of the user to remove.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * member is not in the household (404), or the removal would leave the
 * household with no owner (400).
 */
export const householdsRemoveHouseholdMemberMutation = (options?: Partial<Options<HouseholdsRemoveHouseholdMemberData>>): UseMutationOptions<HouseholdsRemoveHouseholdMemberResponse, HouseholdsRemoveHouseholdMemberError, Options<HouseholdsRemoveHouseholdMemberData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsRemoveHouseholdMemberResponse, HouseholdsRemoveHouseholdMemberError, Options<HouseholdsRemoveHouseholdMemberData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsRemoveHouseholdMember({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Update Household Member
 *
 * Change the role of a household member.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * user_id: The ID of the user whose role changes.
 * member_in: The new role.
 *
 * Returns:
 * The updated membership.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * member is not in the household (404), or the change would leave the
 * household with no owner (400).
 */
export const householdsUpdateHouseholdMemberMutation = (options?: Partial<Options<HouseholdsUpdateHouseholdMemberData>>): UseMutationOptions<HouseholdsUpdateHouseholdMemberResponse, HouseholdsUpdateHouseholdMemberError, Options<HouseholdsUpdateHouseholdMemberData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsUpdateHouseholdMemberResponse, HouseholdsUpdateHouseholdMemberError, Options<HouseholdsUpdateHouseholdMemberData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsUpdateHouseholdMember({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const householdsListHouseholdInvitesQueryKey = (options?: Options<HouseholdsListHouseholdInvitesData>) => createQueryKey('householdsListHouseholdInvites', options);

/**
 * List Household Invites
 *
 * List the invitations sent from this household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 * invite_status: An optional status to filter on.
 *
 * Returns:
 * The invitations, newest first.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsListHouseholdInvitesOptions = (options?: Options<HouseholdsListHouseholdInvitesData>) => queryOptions<HouseholdsListHouseholdInvitesResponse, HouseholdsListHouseholdInvitesError, HouseholdsListHouseholdInvitesResponse, ReturnType<typeof householdsListHouseholdInvitesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await householdsListHouseholdInvites({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: householdsListHouseholdInvitesQueryKey(options)
});

/**
 * Create Household Invite
 *
 * Invite someone to share the household, and email them a link.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * invite_in: The address to invite and the role to give them.
 *
 * Returns:
 * The created invitation.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * household no longer exists (404), or that address already has an
 * invitation or is already a member (409).
 */
export const householdsCreateHouseholdInviteMutation = (options?: Partial<Options<HouseholdsCreateHouseholdInviteData>>): UseMutationOptions<HouseholdsCreateHouseholdInviteResponse, HouseholdsCreateHouseholdInviteError, Options<HouseholdsCreateHouseholdInviteData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsCreateHouseholdInviteResponse, HouseholdsCreateHouseholdInviteError, Options<HouseholdsCreateHouseholdInviteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsCreateHouseholdInvite({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Revoke Household Invite
 *
 * Withdraw an invitation that has not been accepted.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * invite_id: The ID of the invitation to withdraw.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * invitation does not exist in the household (404), or it was already
 * accepted or withdrawn (400).
 */
export const householdsRevokeHouseholdInviteMutation = (options?: Partial<Options<HouseholdsRevokeHouseholdInviteData>>): UseMutationOptions<HouseholdsRevokeHouseholdInviteResponse, HouseholdsRevokeHouseholdInviteError, Options<HouseholdsRevokeHouseholdInviteData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsRevokeHouseholdInviteResponse, HouseholdsRevokeHouseholdInviteError, Options<HouseholdsRevokeHouseholdInviteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsRevokeHouseholdInvite({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Accept Household Invite
 *
 * Accept an invitation and join the household.
 *
 * The household created when you signed up is discarded if it is still
 * empty. If you have already recorded anything in it, joining is refused
 * rather than silently leaving that behind.
 *
 * Args:
 * household_service: The household service dependency.
 * category_service: The category service dependency.
 * current_user: The current authenticated user.
 * accept_in: The invitation token.
 *
 * Returns:
 * The household you joined.
 *
 * Raises:
 * HTTPException: If the token is not recognised (404), the invitation was
 * sent to a different address (403), it has expired or was already
 * used (400), or your current household holds data (409).
 */
export const householdsAcceptHouseholdInviteMutation = (options?: Partial<Options<HouseholdsAcceptHouseholdInviteData>>): UseMutationOptions<HouseholdsAcceptHouseholdInviteResponse, HouseholdsAcceptHouseholdInviteError, Options<HouseholdsAcceptHouseholdInviteData>> => {
    const mutationOptions: UseMutationOptions<HouseholdsAcceptHouseholdInviteResponse, HouseholdsAcceptHouseholdInviteError, Options<HouseholdsAcceptHouseholdInviteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await householdsAcceptHouseholdInvite({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const householdsPreviewHouseholdInviteQueryKey = (options: Options<HouseholdsPreviewHouseholdInviteData>) => createQueryKey('householdsPreviewHouseholdInvite', options);

/**
 * Preview Household Invite
 *
 * Describe an invitation, for the page that offers to accept it.
 *
 * Public, because the recipient may not have an account yet. It carries only
 * what somebody needs in order to decide, and nothing about the household's
 * money.
 *
 * Args:
 * household_service: The household service dependency.
 * token: The invitation token.
 *
 * Returns:
 * The household name, who invited them, and when it expires.
 *
 * Raises:
 * HTTPException: If the token is not recognised (404), or the invitation
 * has expired or was already used (400).
 */
export const householdsPreviewHouseholdInviteOptions = (options: Options<HouseholdsPreviewHouseholdInviteData>) => queryOptions<HouseholdsPreviewHouseholdInviteResponse, HouseholdsPreviewHouseholdInviteError, HouseholdsPreviewHouseholdInviteResponse, ReturnType<typeof householdsPreviewHouseholdInviteQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await householdsPreviewHouseholdInvite({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: householdsPreviewHouseholdInviteQueryKey(options)
});

export const categoriesListCategoriesQueryKey = (options?: Options<CategoriesListCategoriesData>) => createQueryKey('categoriesListCategories', options);

/**
 * List Categories
 *
 * List the categories of the household as a flat list.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived categories.
 * kind: An optional kind to filter on.
 * parent_id: An optional parent to filter on.
 *
 * Returns:
 * The categories.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const categoriesListCategoriesOptions = (options?: Options<CategoriesListCategoriesData>) => queryOptions<CategoriesListCategoriesResponse, CategoriesListCategoriesError, CategoriesListCategoriesResponse, ReturnType<typeof categoriesListCategoriesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await categoriesListCategories({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: categoriesListCategoriesQueryKey(options)
});

/**
 * Create Category
 *
 * Create a category or subcategory.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_in: The category to create.
 *
 * Returns:
 * The created category.
 *
 * Raises:
 * HTTPException: If a sibling already has that name (409), or the parent is
 * missing (404) or invalid (400).
 */
export const categoriesCreateCategoryMutation = (options?: Partial<Options<CategoriesCreateCategoryData>>): UseMutationOptions<CategoriesCreateCategoryResponse, CategoriesCreateCategoryError, Options<CategoriesCreateCategoryData>> => {
    const mutationOptions: UseMutationOptions<CategoriesCreateCategoryResponse, CategoriesCreateCategoryError, Options<CategoriesCreateCategoryData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await categoriesCreateCategory({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const categoriesGetCategoryTreeQueryKey = (options?: Options<CategoriesGetCategoryTreeData>) => createQueryKey('categoriesGetCategoryTree', options);

/**
 * Get Category Tree
 *
 * Get the categories of the household as a two level tree.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived categories.
 * kind: An optional kind to filter on.
 *
 * Returns:
 * The top level categories, each with its subcategories.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const categoriesGetCategoryTreeOptions = (options?: Options<CategoriesGetCategoryTreeData>) => queryOptions<CategoriesGetCategoryTreeResponse, CategoriesGetCategoryTreeError, CategoriesGetCategoryTreeResponse, ReturnType<typeof categoriesGetCategoryTreeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await categoriesGetCategoryTree({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: categoriesGetCategoryTreeQueryKey(options)
});

/**
 * Delete Category
 *
 * Delete a category that nothing references.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404), it
 * still has subcategories (409), or it is built in (400).
 */
export const categoriesDeleteCategoryMutation = (options?: Partial<Options<CategoriesDeleteCategoryData>>): UseMutationOptions<CategoriesDeleteCategoryResponse, CategoriesDeleteCategoryError, Options<CategoriesDeleteCategoryData>> => {
    const mutationOptions: UseMutationOptions<CategoriesDeleteCategoryResponse, CategoriesDeleteCategoryError, Options<CategoriesDeleteCategoryData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await categoriesDeleteCategory({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const categoriesGetCategoryQueryKey = (options: Options<CategoriesGetCategoryData>) => createQueryKey('categoriesGetCategory', options);

/**
 * Get Category
 *
 * Get one category of the household.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category.
 *
 * Returns:
 * The category.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404).
 */
export const categoriesGetCategoryOptions = (options: Options<CategoriesGetCategoryData>) => queryOptions<CategoriesGetCategoryResponse, CategoriesGetCategoryError, CategoriesGetCategoryResponse, ReturnType<typeof categoriesGetCategoryQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await categoriesGetCategory({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: categoriesGetCategoryQueryKey(options)
});

/**
 * Update Category
 *
 * Rename, re-parent, archive or restore a category.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category to update.
 * category_in: The fields to update.
 *
 * Returns:
 * The updated category.
 *
 * Raises:
 * HTTPException: If the category or the new parent does not exist in the
 * household (404), a sibling already has the new name (409), or the
 * change is not allowed (400).
 */
export const categoriesUpdateCategoryMutation = (options?: Partial<Options<CategoriesUpdateCategoryData>>): UseMutationOptions<CategoriesUpdateCategoryResponse, CategoriesUpdateCategoryError, Options<CategoriesUpdateCategoryData>> => {
    const mutationOptions: UseMutationOptions<CategoriesUpdateCategoryResponse, CategoriesUpdateCategoryError, Options<CategoriesUpdateCategoryData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await categoriesUpdateCategory({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const accountsListAccountsQueryKey = (options?: Options<AccountsListAccountsData>) => createQueryKey('accountsListAccounts', options);

/**
 * List Accounts
 *
 * List the accounts of the household with their balances.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived accounts.
 * account_type: An optional type to filter on.
 * skip: Number of records to skip.
 * limit: Maximum number of records to return.
 *
 * Returns:
 * The accounts, and the total of the balances returned.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const accountsListAccountsOptions = (options?: Options<AccountsListAccountsData>) => queryOptions<AccountsListAccountsResponse, AccountsListAccountsError, AccountsListAccountsResponse, ReturnType<typeof accountsListAccountsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await accountsListAccounts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsListAccountsQueryKey(options)
});

/**
 * Create Account
 *
 * Create an account.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_in: The account to create, with the balance it already held.
 *
 * Returns:
 * The created account.
 *
 * Raises:
 * HTTPException: If the household already has an account with that name
 * (409), or the household no longer exists (404).
 */
export const accountsCreateAccountMutation = (options?: Partial<Options<AccountsCreateAccountData>>): UseMutationOptions<AccountsCreateAccountResponse, AccountsCreateAccountError, Options<AccountsCreateAccountData>> => {
    const mutationOptions: UseMutationOptions<AccountsCreateAccountResponse, AccountsCreateAccountError, Options<AccountsCreateAccountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await accountsCreateAccount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Account
 *
 * Delete an account that has no history.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404), or
 * it still has transactions (409).
 */
export const accountsDeleteAccountMutation = (options?: Partial<Options<AccountsDeleteAccountData>>): UseMutationOptions<AccountsDeleteAccountResponse, AccountsDeleteAccountError, Options<AccountsDeleteAccountData>> => {
    const mutationOptions: UseMutationOptions<AccountsDeleteAccountResponse, AccountsDeleteAccountError, Options<AccountsDeleteAccountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await accountsDeleteAccount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const accountsGetAccountQueryKey = (options: Options<AccountsGetAccountData>) => createQueryKey('accountsGetAccount', options);

/**
 * Get Account
 *
 * Get one account of the household with its balance.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account.
 *
 * Returns:
 * The account.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404).
 */
export const accountsGetAccountOptions = (options: Options<AccountsGetAccountData>) => queryOptions<AccountsGetAccountResponse, AccountsGetAccountError, AccountsGetAccountResponse, ReturnType<typeof accountsGetAccountQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await accountsGetAccount({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsGetAccountQueryKey(options)
});

/**
 * Update Account
 *
 * Rename, re-type, archive or restore an account.
 *
 * The opening balance cannot be changed, since that would rewrite every
 * historical balance. Record an adjustment transaction instead.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account to update.
 * account_in: The fields to update.
 *
 * Returns:
 * The updated account.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404), or
 * the household already has an account with the new name (409).
 */
export const accountsUpdateAccountMutation = (options?: Partial<Options<AccountsUpdateAccountData>>): UseMutationOptions<AccountsUpdateAccountResponse, AccountsUpdateAccountError, Options<AccountsUpdateAccountData>> => {
    const mutationOptions: UseMutationOptions<AccountsUpdateAccountResponse, AccountsUpdateAccountError, Options<AccountsUpdateAccountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await accountsUpdateAccount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const transactionsListTransactionsQueryKey = (options?: Options<TransactionsListTransactionsData>) => createQueryKey('transactionsListTransactions', options);

/**
 * List Transactions
 *
 * List the transactions of the household.
 *
 * Any recurring transactions that have fallen due are recorded first, so the
 * ledger is up to date without needing a scheduler.
 *
 * Filtering by a parent category includes the spending filed under its
 * subcategories, unless include_subcategories is set to false.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * recurring_rule_service: The recurring rule service dependency, used to
 * bring the ledger up to date before reading it.
 * household: The current household context.
 * filters: The date range, account, category, kind, amount range, search
 * text, ordering and paging to apply.
 *
 * Returns:
 * The matching transactions and the total number of matches.
 *
 * Raises:
 * HTTPException: If the category filter names a category outside the
 * household (404), or a filter is not recognised (422).
 */
export const transactionsListTransactionsOptions = (options?: Options<TransactionsListTransactionsData>) => queryOptions<TransactionsListTransactionsResponse, TransactionsListTransactionsError, TransactionsListTransactionsResponse, ReturnType<typeof transactionsListTransactionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await transactionsListTransactions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: transactionsListTransactionsQueryKey(options)
});

/**
 * Create Transaction
 *
 * Record an expense, some income, or a transfer between accounts.
 *
 * A transfer is an ordinary transaction with kind "transfer" and a
 * destination account, so it needs no separate endpoint.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_in: The transaction to record.
 *
 * Returns:
 * The recorded transaction.
 *
 * Raises:
 * HTTPException: If an account or category does not exist in the household
 * (404), or the transaction does not match its kind (400).
 */
export const transactionsCreateTransactionMutation = (options?: Partial<Options<TransactionsCreateTransactionData>>): UseMutationOptions<TransactionsCreateTransactionResponse, TransactionsCreateTransactionError, Options<TransactionsCreateTransactionData>> => {
    const mutationOptions: UseMutationOptions<TransactionsCreateTransactionResponse, TransactionsCreateTransactionError, Options<TransactionsCreateTransactionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await transactionsCreateTransaction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Transaction
 *
 * Delete a transaction.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the transaction does not exist in the household (404).
 */
export const transactionsDeleteTransactionMutation = (options?: Partial<Options<TransactionsDeleteTransactionData>>): UseMutationOptions<TransactionsDeleteTransactionResponse, TransactionsDeleteTransactionError, Options<TransactionsDeleteTransactionData>> => {
    const mutationOptions: UseMutationOptions<TransactionsDeleteTransactionResponse, TransactionsDeleteTransactionError, Options<TransactionsDeleteTransactionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await transactionsDeleteTransaction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const transactionsGetTransactionQueryKey = (options: Options<TransactionsGetTransactionData>) => createQueryKey('transactionsGetTransaction', options);

/**
 * Get Transaction
 *
 * Get one transaction of the household.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction.
 *
 * Returns:
 * The transaction.
 *
 * Raises:
 * HTTPException: If the transaction does not exist in the household (404).
 */
export const transactionsGetTransactionOptions = (options: Options<TransactionsGetTransactionData>) => queryOptions<TransactionsGetTransactionResponse, TransactionsGetTransactionError, TransactionsGetTransactionResponse, ReturnType<typeof transactionsGetTransactionQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await transactionsGetTransaction({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: transactionsGetTransactionQueryKey(options)
});

/**
 * Update Transaction
 *
 * Edit a transaction.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction to edit.
 * transaction_in: The fields to change.
 *
 * Returns:
 * The updated transaction.
 *
 * Raises:
 * HTTPException: If the transaction, an account or a category does not
 * exist in the household (404), or the change does not match the kind
 * (400).
 */
export const transactionsUpdateTransactionMutation = (options?: Partial<Options<TransactionsUpdateTransactionData>>): UseMutationOptions<TransactionsUpdateTransactionResponse, TransactionsUpdateTransactionError, Options<TransactionsUpdateTransactionData>> => {
    const mutationOptions: UseMutationOptions<TransactionsUpdateTransactionResponse, TransactionsUpdateTransactionError, Options<TransactionsUpdateTransactionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await transactionsUpdateTransaction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const budgetsListBudgetsQueryKey = (options: Options<BudgetsListBudgetsData>) => createQueryKey('budgetsListBudgets', options);

/**
 * List Budgets
 *
 * List the budgets of the household for one month.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * The budgets, and the total of the limits.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const budgetsListBudgetsOptions = (options: Options<BudgetsListBudgetsData>) => queryOptions<BudgetsListBudgetsResponse, BudgetsListBudgetsError, BudgetsListBudgetsResponse, ReturnType<typeof budgetsListBudgetsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await budgetsListBudgets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: budgetsListBudgetsQueryKey(options)
});

/**
 * Create Budget
 *
 * Set the spending limit of a category for a month.
 *
 * A limit on a parent category covers the spending filed under its
 * subcategories, so a parent and one of its children cannot both be
 * budgeted in the same month.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_in: The category, month and limit.
 *
 * Returns:
 * The created budget.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404),
 * the category or an overlapping one is already budgeted that month
 * (409), or the category is an income category (400).
 */
export const budgetsCreateBudgetMutation = (options?: Partial<Options<BudgetsCreateBudgetData>>): UseMutationOptions<BudgetsCreateBudgetResponse, BudgetsCreateBudgetError, Options<BudgetsCreateBudgetData>> => {
    const mutationOptions: UseMutationOptions<BudgetsCreateBudgetResponse, BudgetsCreateBudgetError, Options<BudgetsCreateBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await budgetsCreateBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Bulk Upsert Budgets
 *
 * Replace the whole set of budgets for one month.
 *
 * This is what a month-at-a-time editing screen saves, so it is one request
 * rather than one per category. A category left out of the set has its limit
 * removed.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * bulk_in: The month and the complete set of limits.
 *
 * Returns:
 * The budgets now set for that month.
 *
 * Raises:
 * HTTPException: If a category does not exist in the household (404), the
 * set budgets both a parent and its subcategory (409), or it names a
 * category twice or an income category (400).
 */
export const budgetsBulkUpsertBudgetsMutation = (options?: Partial<Options<BudgetsBulkUpsertBudgetsData>>): UseMutationOptions<BudgetsBulkUpsertBudgetsResponse, BudgetsBulkUpsertBudgetsError, Options<BudgetsBulkUpsertBudgetsData>> => {
    const mutationOptions: UseMutationOptions<BudgetsBulkUpsertBudgetsResponse, BudgetsBulkUpsertBudgetsError, Options<BudgetsBulkUpsertBudgetsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await budgetsBulkUpsertBudgets({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Copy Budgets
 *
 * Copy the budgets of one month onto another.
 *
 * Budgets do not roll over, so this is what makes "same as last month" a
 * single action rather than retyping every limit.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * copy_in: The source month, the target month, and whether to replace
 * limits already set on the target.
 *
 * Returns:
 * The budgets now set for the target month.
 *
 * Raises:
 * HTTPException: If the target month already has budgets and overwrite was
 * not requested (409).
 */
export const budgetsCopyBudgetsMutation = (options?: Partial<Options<BudgetsCopyBudgetsData>>): UseMutationOptions<BudgetsCopyBudgetsResponse, BudgetsCopyBudgetsError, Options<BudgetsCopyBudgetsData>> => {
    const mutationOptions: UseMutationOptions<BudgetsCopyBudgetsResponse, BudgetsCopyBudgetsError, Options<BudgetsCopyBudgetsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await budgetsCopyBudgets({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Budget
 *
 * Remove a budget.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget to remove.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsDeleteBudgetMutation = (options?: Partial<Options<BudgetsDeleteBudgetData>>): UseMutationOptions<BudgetsDeleteBudgetResponse, BudgetsDeleteBudgetError, Options<BudgetsDeleteBudgetData>> => {
    const mutationOptions: UseMutationOptions<BudgetsDeleteBudgetResponse, BudgetsDeleteBudgetError, Options<BudgetsDeleteBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await budgetsDeleteBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const budgetsGetBudgetQueryKey = (options: Options<BudgetsGetBudgetData>) => createQueryKey('budgetsGetBudget', options);

/**
 * Get Budget
 *
 * Get one budget of the household.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget.
 *
 * Returns:
 * The budget.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsGetBudgetOptions = (options: Options<BudgetsGetBudgetData>) => queryOptions<BudgetsGetBudgetResponse, BudgetsGetBudgetError, BudgetsGetBudgetResponse, ReturnType<typeof budgetsGetBudgetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await budgetsGetBudget({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: budgetsGetBudgetQueryKey(options)
});

/**
 * Update Budget
 *
 * Change the limit of a budget.
 *
 * The category and month identify the budget, so changing either means
 * setting a different budget rather than editing this one.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget to change.
 * budget_in: The new limit.
 *
 * Returns:
 * The updated budget.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsUpdateBudgetMutation = (options?: Partial<Options<BudgetsUpdateBudgetData>>): UseMutationOptions<BudgetsUpdateBudgetResponse, BudgetsUpdateBudgetError, Options<BudgetsUpdateBudgetData>> => {
    const mutationOptions: UseMutationOptions<BudgetsUpdateBudgetResponse, BudgetsUpdateBudgetError, Options<BudgetsUpdateBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await budgetsUpdateBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const recurringRulesListRecurringRulesQueryKey = (options?: Options<RecurringRulesListRecurringRulesData>) => createQueryKey('recurringRulesListRecurringRules', options);

/**
 * List Recurring Rules
 *
 * List the recurring rules of the household.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * is_active: An optional active state to filter on.
 * skip: Number of records to skip.
 * limit: Maximum number of records to return.
 *
 * Returns:
 * The rules, soonest due first.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const recurringRulesListRecurringRulesOptions = (options?: Options<RecurringRulesListRecurringRulesData>) => queryOptions<RecurringRulesListRecurringRulesResponse, RecurringRulesListRecurringRulesError, RecurringRulesListRecurringRulesResponse, ReturnType<typeof recurringRulesListRecurringRulesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await recurringRulesListRecurringRules({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: recurringRulesListRecurringRulesQueryKey(options)
});

/**
 * Create Recurring Rule
 *
 * Create a recurring rule, such as rent or a subscription.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_in: The rule to create.
 *
 * Returns:
 * The created rule, with the date it first falls due.
 *
 * Raises:
 * HTTPException: If an account or category does not exist in the household
 * (404), or the rule does not match its kind or would never come due
 * (400).
 */
export const recurringRulesCreateRecurringRuleMutation = (options?: Partial<Options<RecurringRulesCreateRecurringRuleData>>): UseMutationOptions<RecurringRulesCreateRecurringRuleResponse, RecurringRulesCreateRecurringRuleError, Options<RecurringRulesCreateRecurringRuleData>> => {
    const mutationOptions: UseMutationOptions<RecurringRulesCreateRecurringRuleResponse, RecurringRulesCreateRecurringRuleError, Options<RecurringRulesCreateRecurringRuleData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await recurringRulesCreateRecurringRule({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const recurringRulesListUpcomingOccurrencesQueryKey = (options?: Options<RecurringRulesListUpcomingOccurrencesData>) => createQueryKey('recurringRulesListUpcomingOccurrences', options);

/**
 * List Upcoming Occurrences
 *
 * Show the recurring occurrences that have not been recorded yet.
 *
 * Read only: nothing is created, so this can be used to plan ahead.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * until: The last day to look ahead to, inclusive. Defaults to two months out.
 *
 * Returns:
 * The projected occurrences, soonest first, and what they add up to.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const recurringRulesListUpcomingOccurrencesOptions = (options?: Options<RecurringRulesListUpcomingOccurrencesData>) => queryOptions<RecurringRulesListUpcomingOccurrencesResponse, RecurringRulesListUpcomingOccurrencesError, RecurringRulesListUpcomingOccurrencesResponse, ReturnType<typeof recurringRulesListUpcomingOccurrencesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await recurringRulesListUpcomingOccurrences({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: recurringRulesListUpcomingOccurrencesQueryKey(options)
});

/**
 * Run Recurring Rules
 *
 * Record the transactions the rules have fallen due for.
 *
 * The read paths do this automatically, so this endpoint exists for catching
 * up explicitly, or for creating occurrences ahead of today.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * until: The last day to record up to, inclusive. Defaults to today.
 *
 * Returns:
 * How many transactions were created and how many rules moved on.
 *
 * Raises:
 * HTTPException: If a rule points at an account that no longer exists (404).
 */
export const recurringRulesRunRecurringRulesMutation = (options?: Partial<Options<RecurringRulesRunRecurringRulesData>>): UseMutationOptions<RecurringRulesRunRecurringRulesResponse, RecurringRulesRunRecurringRulesError, Options<RecurringRulesRunRecurringRulesData>> => {
    const mutationOptions: UseMutationOptions<RecurringRulesRunRecurringRulesResponse, RecurringRulesRunRecurringRulesError, Options<RecurringRulesRunRecurringRulesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await recurringRulesRunRecurringRules({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Recurring Rule
 *
 * Delete a recurring rule.
 *
 * The transactions it already created are kept: they happened, and the rule
 * is only the template that made them.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the rule does not exist in the household (404).
 */
export const recurringRulesDeleteRecurringRuleMutation = (options?: Partial<Options<RecurringRulesDeleteRecurringRuleData>>): UseMutationOptions<RecurringRulesDeleteRecurringRuleResponse, RecurringRulesDeleteRecurringRuleError, Options<RecurringRulesDeleteRecurringRuleData>> => {
    const mutationOptions: UseMutationOptions<RecurringRulesDeleteRecurringRuleResponse, RecurringRulesDeleteRecurringRuleError, Options<RecurringRulesDeleteRecurringRuleData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await recurringRulesDeleteRecurringRule({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const recurringRulesGetRecurringRuleQueryKey = (options: Options<RecurringRulesGetRecurringRuleData>) => createQueryKey('recurringRulesGetRecurringRule', options);

/**
 * Get Recurring Rule
 *
 * Get one recurring rule of the household.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule.
 *
 * Returns:
 * The rule.
 *
 * Raises:
 * HTTPException: If the rule does not exist in the household (404).
 */
export const recurringRulesGetRecurringRuleOptions = (options: Options<RecurringRulesGetRecurringRuleData>) => queryOptions<RecurringRulesGetRecurringRuleResponse, RecurringRulesGetRecurringRuleError, RecurringRulesGetRecurringRuleResponse, ReturnType<typeof recurringRulesGetRecurringRuleQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await recurringRulesGetRecurringRule({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: recurringRulesGetRecurringRuleQueryKey(options)
});

/**
 * Update Recurring Rule
 *
 * Edit a recurring rule, or pause it by setting is_active to false.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule to edit.
 * rule_in: The fields to change.
 *
 * Returns:
 * The updated rule.
 *
 * Raises:
 * HTTPException: If the rule or the category does not exist in the
 * household (404), or the new schedule would never come due (400).
 */
export const recurringRulesUpdateRecurringRuleMutation = (options?: Partial<Options<RecurringRulesUpdateRecurringRuleData>>): UseMutationOptions<RecurringRulesUpdateRecurringRuleResponse, RecurringRulesUpdateRecurringRuleError, Options<RecurringRulesUpdateRecurringRuleData>> => {
    const mutationOptions: UseMutationOptions<RecurringRulesUpdateRecurringRuleResponse, RecurringRulesUpdateRecurringRuleError, Options<RecurringRulesUpdateRecurringRuleData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await recurringRulesUpdateRecurringRule({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const reportsSpendByCategoryQueryKey = (options: Options<ReportsSpendByCategoryData>) => createQueryKey('reportsSpendByCategory', options);

/**
 * Spend By Category
 *
 * Break a month's spending down by category.
 *
 * Transfers can never appear here: they are their own kind, so moving money
 * between your own accounts is not spending.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 * kind: Whether to break down expenses or income.
 * depth: Whether to report per subcategory, or rolled up to the parent.
 *
 * Returns:
 * One slice per category, largest first, with each share of the total.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsSpendByCategoryOptions = (options: Options<ReportsSpendByCategoryData>) => queryOptions<ReportsSpendByCategoryResponse, ReportsSpendByCategoryError, ReportsSpendByCategoryResponse, ReturnType<typeof reportsSpendByCategoryQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reportsSpendByCategory({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reportsSpendByCategoryQueryKey(options)
});

export const reportsSpendOverTimeQueryKey = (options: Options<ReportsSpendOverTimeData>) => createQueryKey('reportsSpendOverTime', options);

/**
 * Spend Over Time
 *
 * Chart spending over a period.
 *
 * Buckets with no activity are returned as zero rather than left out, so the
 * chart cannot imply a gap in time that is not there.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * date_from: First day of the period, inclusive.
 * date_to: Last day of the period, inclusive.
 * granularity: Whether to bucket by day or by month.
 * kind: Whether to chart expenses or income.
 * account_id: An optional account to restrict to.
 * category_id: An optional category to restrict to. A parent category
 * includes the spending filed under its subcategories.
 *
 * Returns:
 * One point per bucket, oldest first.
 *
 * Raises:
 * HTTPException: If the range starts after it ends (400), the range is too
 * large for daily buckets (422), or the category does not exist in the
 * household (404).
 */
export const reportsSpendOverTimeOptions = (options: Options<ReportsSpendOverTimeData>) => queryOptions<ReportsSpendOverTimeResponse, ReportsSpendOverTimeError, ReportsSpendOverTimeResponse, ReturnType<typeof reportsSpendOverTimeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reportsSpendOverTime({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reportsSpendOverTimeQueryKey(options)
});

export const reportsIncomeExpenseQueryKey = (options: Options<ReportsIncomeExpenseData>) => createQueryKey('reportsIncomeExpense', options);

/**
 * Income Expense
 *
 * Compare money in against money out, month by month.
 *
 * Each month also carries the running net, which is the savings trend.
 * Transfers appear on neither side.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month_from: First month of the range, in "YYYY-MM" form.
 * month_to: Last month of the range, in "YYYY-MM" form.
 *
 * Returns:
 * One entry per month, oldest first, plus the totals for the range.
 *
 * Raises:
 * HTTPException: If the range starts after it ends (400), or covers more
 * than ten years (422).
 */
export const reportsIncomeExpenseOptions = (options: Options<ReportsIncomeExpenseData>) => queryOptions<ReportsIncomeExpenseResponse, ReportsIncomeExpenseError, ReportsIncomeExpenseResponse, ReturnType<typeof reportsIncomeExpenseQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reportsIncomeExpense({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reportsIncomeExpenseQueryKey(options)
});

export const reportsBudgetProgressQueryKey = (options: Options<ReportsBudgetProgressData>) => createQueryKey('reportsBudgetProgress', options);

/**
 * Budget Progress
 *
 * Compare a month's spending against the limits set for it.
 *
 * A limit on a parent category counts the spending filed under its
 * subcategories, so budgeting "Food & Drink" tracks what was spent on
 * groceries and restaurants together.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * One row per budget, sorted by how much of each limit is used, plus the
 * spending that had no limit at all.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsBudgetProgressOptions = (options: Options<ReportsBudgetProgressData>) => queryOptions<ReportsBudgetProgressResponse, ReportsBudgetProgressError, ReportsBudgetProgressResponse, ReturnType<typeof reportsBudgetProgressQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reportsBudgetProgress({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reportsBudgetProgressQueryKey(options)
});

export const reportsMonthSummaryQueryKey = (options: Options<ReportsMonthSummaryData>) => createQueryKey('reportsMonthSummary', options);

/**
 * Month Summary
 *
 * Gather the dashboard figures for one month.
 *
 * One request rather than several, so opening the app is a single round trip.
 * Any recurring transactions that have fallen due are recorded first, so the
 * dashboard is not out of date the moment it loads.
 *
 * Args:
 * report_service: The report service dependency.
 * recurring_rule_service: The recurring rule service dependency, used to
 * bring the ledger up to date before reading it.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * The month's income, spending and net, the current net worth, how much
 * was budgeted, how many categories went over, and the biggest categories.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsMonthSummaryOptions = (options: Options<ReportsMonthSummaryData>) => queryOptions<ReportsMonthSummaryResponse, ReportsMonthSummaryError, ReportsMonthSummaryResponse, ReturnType<typeof reportsMonthSummaryQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reportsMonthSummary({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reportsMonthSummaryQueryKey(options)
});

/**
 * Login Access Token
 *
 * Get an access token for future requests.
 *
 * The obtained token could be used as a Bearer token in subsequent authenticated requests.
 *
 * Args:
 * user_service: The user service dependency.
 * email_verification_service: The email verification service dependency.
 * form_data: The OAuth2 password request form data.
 *
 * Returns:
 * A token containing the access token.
 *
 * Raises:
 * HTTPException: If the credentials do not check out (401), or the user is inactive (403). The 401
 * is deliberately the same whether or not the address is registered, so that the endpoint cannot
 * be used to enumerate accounts. For 403, the error message will indicate if email verification
 * is pending.
 */
export const loginLoginAccessTokenMutation = (options?: Partial<Options<LoginLoginAccessTokenData>>): UseMutationOptions<LoginLoginAccessTokenResponse, LoginLoginAccessTokenError, Options<LoginLoginAccessTokenData>> => {
    const mutationOptions: UseMutationOptions<LoginLoginAccessTokenResponse, LoginLoginAccessTokenError, Options<LoginLoginAccessTokenData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await loginLoginAccessToken({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Request Password Reset
 *
 * Request a password reset.
 *
 * For security reasons, this always returns success even if the email doesn't exist.
 * This prevents user enumeration attacks.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * user_service: The user service dependency.
 * password_reset_in: The password reset request payload.
 *
 * Returns:
 * A message indicating that the request was successful.
 */
export const passwordResetRequestPasswordResetMutation = (options?: Partial<Options<PasswordResetRequestPasswordResetData>>): UseMutationOptions<PasswordResetRequestPasswordResetResponse, PasswordResetRequestPasswordResetError, Options<PasswordResetRequestPasswordResetData>> => {
    const mutationOptions: UseMutationOptions<PasswordResetRequestPasswordResetResponse, PasswordResetRequestPasswordResetError, Options<PasswordResetRequestPasswordResetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await passwordResetRequestPasswordReset({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Verify Password Reset Token
 *
 * Verify a password reset token is valid.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * password_reset_verify: The password reset verification payload.
 *
 * Returns:
 * A message indicating that the token is valid.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), used (400), or not found (404).
 */
export const passwordResetVerifyPasswordResetTokenMutation = (options?: Partial<Options<PasswordResetVerifyPasswordResetTokenData>>): UseMutationOptions<PasswordResetVerifyPasswordResetTokenResponse, PasswordResetVerifyPasswordResetTokenError, Options<PasswordResetVerifyPasswordResetTokenData>> => {
    const mutationOptions: UseMutationOptions<PasswordResetVerifyPasswordResetTokenResponse, PasswordResetVerifyPasswordResetTokenError, Options<PasswordResetVerifyPasswordResetTokenData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await passwordResetVerifyPasswordResetToken({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Confirm Password Reset
 *
 * Reset a user's password.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * user_service: The user service dependency.
 * password_reset_confirm: The password reset confirmation payload.
 *
 * Returns:
 * A message indicating that the password was reset successfully.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), used (400), or not found (404).
 */
export const passwordResetConfirmPasswordResetMutation = (options?: Partial<Options<PasswordResetConfirmPasswordResetData>>): UseMutationOptions<PasswordResetConfirmPasswordResetResponse, PasswordResetConfirmPasswordResetError, Options<PasswordResetConfirmPasswordResetData>> => {
    const mutationOptions: UseMutationOptions<PasswordResetConfirmPasswordResetResponse, PasswordResetConfirmPasswordResetError, Options<PasswordResetConfirmPasswordResetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await passwordResetConfirmPasswordReset({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Resend Verification Email
 *
 * Send or resend an email verification.
 *
 * For security reasons, this always returns success even if the email doesn't exist.
 * This prevents user enumeration attacks.
 *
 * Args:
 * email_verification_service: The email verification service dependency.
 * user_service: The user service dependency.
 * email_verification_request: The email verification request payload.
 *
 * Returns:
 * A message indicating that the request was successful.
 */
export const emailVerificationResendVerificationEmailMutation = (options?: Partial<Options<EmailVerificationResendVerificationEmailData>>): UseMutationOptions<EmailVerificationResendVerificationEmailResponse, EmailVerificationResendVerificationEmailError, Options<EmailVerificationResendVerificationEmailData>> => {
    const mutationOptions: UseMutationOptions<EmailVerificationResendVerificationEmailResponse, EmailVerificationResendVerificationEmailError, Options<EmailVerificationResendVerificationEmailData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await emailVerificationResendVerificationEmail({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Verify Email
 *
 * Verify a user's email address.
 *
 * Args:
 * email_verification_service: The email verification service dependency.
 * user_service: The user service dependency.
 * email_verification_confirm: The email verification confirmation payload.
 *
 * Returns:
 * A message indicating that the email was verified successfully.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), already used (400), or not found (404).
 */
export const emailVerificationVerifyEmailMutation = (options?: Partial<Options<EmailVerificationVerifyEmailData>>): UseMutationOptions<EmailVerificationVerifyEmailResponse, EmailVerificationVerifyEmailError, Options<EmailVerificationVerifyEmailData>> => {
    const mutationOptions: UseMutationOptions<EmailVerificationVerifyEmailResponse, EmailVerificationVerifyEmailError, Options<EmailVerificationVerifyEmailData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await emailVerificationVerifyEmail({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
