// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, type Options as Options2, type RequestResult, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { AccountsCreateAccountData, AccountsCreateAccountErrors, AccountsCreateAccountResponses, AccountsDeleteAccountData, AccountsDeleteAccountErrors, AccountsDeleteAccountResponses, AccountsGetAccountData, AccountsGetAccountErrors, AccountsGetAccountResponses, AccountsListAccountsData, AccountsListAccountsErrors, AccountsListAccountsResponses, AccountsUpdateAccountData, AccountsUpdateAccountErrors, AccountsUpdateAccountResponses, BudgetsBulkUpsertBudgetsData, BudgetsBulkUpsertBudgetsErrors, BudgetsBulkUpsertBudgetsResponses, BudgetsCopyBudgetsData, BudgetsCopyBudgetsErrors, BudgetsCopyBudgetsResponses, BudgetsCreateBudgetData, BudgetsCreateBudgetErrors, BudgetsCreateBudgetResponses, BudgetsDeleteBudgetData, BudgetsDeleteBudgetErrors, BudgetsDeleteBudgetResponses, BudgetsGetBudgetData, BudgetsGetBudgetErrors, BudgetsGetBudgetResponses, BudgetsListBudgetsData, BudgetsListBudgetsErrors, BudgetsListBudgetsResponses, BudgetsUpdateBudgetData, BudgetsUpdateBudgetErrors, BudgetsUpdateBudgetResponses, CategoriesCreateCategoryData, CategoriesCreateCategoryErrors, CategoriesCreateCategoryResponses, CategoriesDeleteCategoryData, CategoriesDeleteCategoryErrors, CategoriesDeleteCategoryResponses, CategoriesGetCategoryData, CategoriesGetCategoryErrors, CategoriesGetCategoryResponses, CategoriesGetCategoryTreeData, CategoriesGetCategoryTreeErrors, CategoriesGetCategoryTreeResponses, CategoriesListCategoriesData, CategoriesListCategoriesErrors, CategoriesListCategoriesResponses, CategoriesUpdateCategoryData, CategoriesUpdateCategoryErrors, CategoriesUpdateCategoryResponses, EmailVerificationResendVerificationEmailData, EmailVerificationResendVerificationEmailErrors, EmailVerificationResendVerificationEmailResponses, EmailVerificationVerifyEmailData, EmailVerificationVerifyEmailErrors, EmailVerificationVerifyEmailResponses, HouseholdsAcceptHouseholdInviteData, HouseholdsAcceptHouseholdInviteErrors, HouseholdsAcceptHouseholdInviteResponses, HouseholdsCreateHouseholdInviteData, HouseholdsCreateHouseholdInviteErrors, HouseholdsCreateHouseholdInviteResponses, HouseholdsGetHouseholdMeData, HouseholdsGetHouseholdMeResponses, HouseholdsLeaveHouseholdData, HouseholdsLeaveHouseholdResponses, HouseholdsListHouseholdInvitesData, HouseholdsListHouseholdInvitesErrors, HouseholdsListHouseholdInvitesResponses, HouseholdsListHouseholdMembersData, HouseholdsListHouseholdMembersResponses, HouseholdsPreviewHouseholdInviteData, HouseholdsPreviewHouseholdInviteErrors, HouseholdsPreviewHouseholdInviteResponses, HouseholdsRemoveHouseholdMemberData, HouseholdsRemoveHouseholdMemberErrors, HouseholdsRemoveHouseholdMemberResponses, HouseholdsRevokeHouseholdInviteData, HouseholdsRevokeHouseholdInviteErrors, HouseholdsRevokeHouseholdInviteResponses, HouseholdsUpdateHouseholdMeData, HouseholdsUpdateHouseholdMeErrors, HouseholdsUpdateHouseholdMemberData, HouseholdsUpdateHouseholdMemberErrors, HouseholdsUpdateHouseholdMemberResponses, HouseholdsUpdateHouseholdMeResponses, LoginLoginAccessTokenData, LoginLoginAccessTokenErrors, LoginLoginAccessTokenResponses, PasswordResetConfirmPasswordResetData, PasswordResetConfirmPasswordResetErrors, PasswordResetConfirmPasswordResetResponses, PasswordResetRequestPasswordResetData, PasswordResetRequestPasswordResetErrors, PasswordResetRequestPasswordResetResponses, PasswordResetVerifyPasswordResetTokenData, PasswordResetVerifyPasswordResetTokenErrors, PasswordResetVerifyPasswordResetTokenResponses, RecurringRulesCreateRecurringRuleData, RecurringRulesCreateRecurringRuleErrors, RecurringRulesCreateRecurringRuleResponses, RecurringRulesDeleteRecurringRuleData, RecurringRulesDeleteRecurringRuleErrors, RecurringRulesDeleteRecurringRuleResponses, RecurringRulesGetRecurringRuleData, RecurringRulesGetRecurringRuleErrors, RecurringRulesGetRecurringRuleResponses, RecurringRulesListRecurringRulesData, RecurringRulesListRecurringRulesErrors, RecurringRulesListRecurringRulesResponses, RecurringRulesListUpcomingOccurrencesData, RecurringRulesListUpcomingOccurrencesErrors, RecurringRulesListUpcomingOccurrencesResponses, RecurringRulesRunRecurringRulesData, RecurringRulesRunRecurringRulesErrors, RecurringRulesRunRecurringRulesResponses, RecurringRulesUpdateRecurringRuleData, RecurringRulesUpdateRecurringRuleErrors, RecurringRulesUpdateRecurringRuleResponses, ReportsBudgetProgressData, ReportsBudgetProgressErrors, ReportsBudgetProgressResponses, ReportsIncomeExpenseData, ReportsIncomeExpenseErrors, ReportsIncomeExpenseResponses, ReportsMonthSummaryData, ReportsMonthSummaryErrors, ReportsMonthSummaryResponses, ReportsSpendByCategoryData, ReportsSpendByCategoryErrors, ReportsSpendByCategoryResponses, ReportsSpendOverTimeData, ReportsSpendOverTimeErrors, ReportsSpendOverTimeResponses, TransactionsCreateTransactionData, TransactionsCreateTransactionErrors, TransactionsCreateTransactionResponses, TransactionsDeleteTransactionData, TransactionsDeleteTransactionErrors, TransactionsDeleteTransactionResponses, TransactionsGetTransactionData, TransactionsGetTransactionErrors, TransactionsGetTransactionResponses, TransactionsListTransactionsData, TransactionsListTransactionsErrors, TransactionsListTransactionsResponses, TransactionsUpdateTransactionData, TransactionsUpdateTransactionErrors, TransactionsUpdateTransactionResponses, UsersCreateUserData, UsersCreateUserErrors, UsersCreateUserResponses, UsersDeleteUserData, UsersDeleteUserErrors, UsersDeleteUserMeData, UsersDeleteUserMeResponses, UsersDeleteUserResponses, UsersGetUserByIdData, UsersGetUserByIdErrors, UsersGetUserByIdResponses, UsersGetUserMeData, UsersGetUserMeResponses, UsersGetUsersData, UsersGetUsersErrors, UsersGetUsersResponses, UsersRegisterUserData, UsersRegisterUserErrors, UsersRegisterUserResponses, UsersUpdatePasswordMeData, UsersUpdatePasswordMeErrors, UsersUpdatePasswordMeResponses, UsersUpdateUserData, UsersUpdateUserErrors, UsersUpdateUserMeData, UsersUpdateUserMeErrors, UsersUpdateUserMeResponses, UsersUpdateUserResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Get Users
 *
 * Get registered users.
 *
 * Args:
 * user_service: The user service dependency.
 * skip (int): The number of users to skip.
 * limit (int): The maximum number of users to return.
 *
 * Returns:
 * A list of users.
 *
 * Raises:
 * HTTPException: If the user is not authorized (403), has an invalid token (401), or is not found (404).
 */
export const usersGetUsers = <ThrowOnError extends boolean = false>(options?: Options<UsersGetUsersData, ThrowOnError>): RequestResult<UsersGetUsersResponses, UsersGetUsersErrors, ThrowOnError> => (options?.client ?? client).get<UsersGetUsersResponses, UsersGetUsersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/',
    ...options
});

/**
 * Create User
 *
 * Create a new user.
 *
 * Args:
 * user_service: The user service dependency.
 * household_service: The household service dependency, used to provision
 * the user's household in the same transaction.
 * category_service: The category service dependency, used to seed the
 * household's default categories.
 * user_in: The user creation payload.
 *
 * Returns:
 * The newly created user.
 *
 * Raises:
 * HTTPException: If a user with this email already exists (409),
 * the user is not authorized to perform this action (403),
 * the user's token is invalid (401), or the authenticated
 * user is not found in the database (404).
 */
export const usersCreateUser = <ThrowOnError extends boolean = false>(options: Options<UsersCreateUserData, ThrowOnError>): RequestResult<UsersCreateUserResponses, UsersCreateUserErrors, ThrowOnError> => (options.client ?? client).post<UsersCreateUserResponses, UsersCreateUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register User
 *
 * Register a new user.
 *
 * This endpoint allows users to register without being authenticated.
 * The user will be created with is_active=False and must verify their email
 * before they can log in.
 *
 * Args:
 * user_service: The user service dependency.
 * email_verification_service: The email verification service dependency.
 * household_service: The household service dependency, used to provision
 * the user's household in the same transaction.
 * category_service: The category service dependency, used to seed the
 * household's default categories.
 * user_in: The user registration data.
 *
 * Returns:
 * The newly created user.
 *
 * Raises:
 * HTTPException: If a user with the same email already exists (409).
 */
export const usersRegisterUser = <ThrowOnError extends boolean = false>(options: Options<UsersRegisterUserData, ThrowOnError>): RequestResult<UsersRegisterUserResponses, UsersRegisterUserErrors, ThrowOnError> => (options.client ?? client).post<UsersRegisterUserResponses, UsersRegisterUserErrors, ThrowOnError>({
    url: '/api/v1/users/signup',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete User Me
 *
 * Delete the current user.
 *
 * Args:
 * user_service: The user service dependency.
 * current_user: The current authenticated user.
 *
 * Returns:
 * A message indicating that the user was deleted successfully.
 *
 * Raises:
 * HTTPException: If a superuser tries to delete their own account (400), the token is invalid (401),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersDeleteUserMe = <ThrowOnError extends boolean = false>(options?: Options<UsersDeleteUserMeData, ThrowOnError>): RequestResult<UsersDeleteUserMeResponses, unknown, ThrowOnError> => (options?.client ?? client).delete<UsersDeleteUserMeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me',
    ...options
});

/**
 * Get User Me
 *
 * Get the current user's information.
 *
 * Args:
 * current_user: The current authenticated user.
 *
 * Returns:
 * The current user's public information.
 *
 * Raises:
 * HTTPException: If the user's token is invalid (401), the user is not found (404), or the user is inactive (403).
 */
export const usersGetUserMe = <ThrowOnError extends boolean = false>(options?: Options<UsersGetUserMeData, ThrowOnError>): RequestResult<UsersGetUserMeResponses, unknown, ThrowOnError> => (options?.client ?? client).get<UsersGetUserMeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me',
    ...options
});

/**
 * Update User Me
 *
 * Update the current user's information.
 *
 * Args:
 * user_service: The user service dependency.
 * password_reset_service: The password reset service dependency.
 * email_verification_service: The email verification service dependency.
 * user_in: The user data to update.
 * current_user: The current authenticated user.
 *
 * Returns:
 * The updated user information.
 *
 * Raises:
 * HTTPException: If a user with the same email already exists (409), the user's token is invalid (401),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersUpdateUserMe = <ThrowOnError extends boolean = false>(options: Options<UsersUpdateUserMeData, ThrowOnError>): RequestResult<UsersUpdateUserMeResponses, UsersUpdateUserMeErrors, ThrowOnError> => (options.client ?? client).patch<UsersUpdateUserMeResponses, UsersUpdateUserMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete User
 *
 * Delete a user.
 *
 * This endpoint is only accessible to superusers.
 *
 * Args:
 * user_service: The user service dependency.
 * user_id: The ID of the user to delete.
 *
 * Returns:
 * A message indicating that the user was deleted successfully.
 *
 * Raises:
 * HTTPException: If the user to delete is not found (404), trying to delete any superuser account (400),
 * the current user is not authorized (403), or the token is invalid (401).
 */
export const usersDeleteUser = <ThrowOnError extends boolean = false>(options: Options<UsersDeleteUserData, ThrowOnError>): RequestResult<UsersDeleteUserResponses, UsersDeleteUserErrors, ThrowOnError> => (options.client ?? client).delete<UsersDeleteUserResponses, UsersDeleteUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}',
    ...options
});

/**
 * Get User By Id
 *
 * Get a specific user by their ID.
 *
 * A user can retrieve their own information. Superusers can retrieve any
 * user's information.
 *
 * Args:
 * user_service: The user service dependency.
 * current_user: The current authenticated user.
 * user_id: The ID of the user to retrieve.
 *
 * Returns:
 * The user's public information.
 *
 * Raises:
 * HTTPException: If the user does not have sufficient privileges (403), the user is not found (404),
 * or the token is invalid (401).
 */
export const usersGetUserById = <ThrowOnError extends boolean = false>(options: Options<UsersGetUserByIdData, ThrowOnError>): RequestResult<UsersGetUserByIdResponses, UsersGetUserByIdErrors, ThrowOnError> => (options.client ?? client).get<UsersGetUserByIdResponses, UsersGetUserByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}',
    ...options
});

/**
 * Update User
 *
 * Update a user's information.
 *
 * This endpoint is only accessible to superusers.
 *
 * Args:
 * user_service: The user service dependency.
 * user_id: The ID of the user to update.
 * user_in: The user data to update.
 *
 * Returns:
 * The updated user information.
 *
 * Raises:
 * HTTPException: If the user is not found (404), the new email already exists (409),
 * the current user is not authorized (403), or the token is invalid (401).
 */
export const usersUpdateUser = <ThrowOnError extends boolean = false>(options: Options<UsersUpdateUserData, ThrowOnError>): RequestResult<UsersUpdateUserResponses, UsersUpdateUserErrors, ThrowOnError> => (options.client ?? client).patch<UsersUpdateUserResponses, UsersUpdateUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Password Me
 *
 * Update the current user's password.
 *
 * Args:
 * user_service: The user service dependency.
 * password_in: The current and new password.
 * current_user: The current authenticated user.
 *
 * Returns:
 * A message indicating that the password was updated successfully.
 *
 * Raises:
 * HTTPException: If the current password is incorrect (401),
 * the new password is the same as the current one (400),
 * the user is not found (404), or the user is inactive (403).
 */
export const usersUpdatePasswordMe = <ThrowOnError extends boolean = false>(options: Options<UsersUpdatePasswordMeData, ThrowOnError>): RequestResult<UsersUpdatePasswordMeResponses, UsersUpdatePasswordMeErrors, ThrowOnError> => (options.client ?? client).patch<UsersUpdatePasswordMeResponses, UsersUpdatePasswordMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me/password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Household Me
 *
 * Get the household of the current user.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 *
 * Returns:
 * The household, with its member count.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsGetHouseholdMe = <ThrowOnError extends boolean = false>(options?: Options<HouseholdsGetHouseholdMeData, ThrowOnError>): RequestResult<HouseholdsGetHouseholdMeResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HouseholdsGetHouseholdMeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me',
    ...options
});

/**
 * Update Household Me
 *
 * Rename the household of the current user.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * household_in: The fields to update.
 *
 * Returns:
 * The updated household.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), or the
 * household no longer exists (404).
 */
export const householdsUpdateHouseholdMe = <ThrowOnError extends boolean = false>(options: Options<HouseholdsUpdateHouseholdMeData, ThrowOnError>): RequestResult<HouseholdsUpdateHouseholdMeResponses, HouseholdsUpdateHouseholdMeErrors, ThrowOnError> => (options.client ?? client).patch<HouseholdsUpdateHouseholdMeResponses, HouseholdsUpdateHouseholdMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Household Members
 *
 * List the members of the household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 *
 * Returns:
 * The members of the household.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsListHouseholdMembers = <ThrowOnError extends boolean = false>(options?: Options<HouseholdsListHouseholdMembersData, ThrowOnError>): RequestResult<HouseholdsListHouseholdMembersResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HouseholdsListHouseholdMembersResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/members',
    ...options
});

/**
 * Leave Household
 *
 * Leave the household.
 *
 * The caller keeps their account and is given a fresh, empty household.
 *
 * Args:
 * household_service: The household service dependency.
 * category_service: The category service dependency, used to seed the
 * categories of the replacement household.
 * household: The current household context.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the caller is the household's only owner (400), or the
 * membership no longer exists (404).
 */
export const householdsLeaveHousehold = <ThrowOnError extends boolean = false>(options?: Options<HouseholdsLeaveHouseholdData, ThrowOnError>): RequestResult<HouseholdsLeaveHouseholdResponses, unknown, ThrowOnError> => (options?.client ?? client).delete<HouseholdsLeaveHouseholdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/members/me',
    ...options
});

/**
 * Remove Household Member
 *
 * Remove a member from the household.
 *
 * The removed member keeps their account and is given a fresh, empty household.
 *
 * Args:
 * household_service: The household service dependency.
 * category_service: The category service dependency, used to seed the
 * categories of the replacement household.
 * household: The current household context, which must be owned by the user.
 * user_id: The ID of the user to remove.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * member is not in the household (404), or the removal would leave the
 * household with no owner (400).
 */
export const householdsRemoveHouseholdMember = <ThrowOnError extends boolean = false>(options: Options<HouseholdsRemoveHouseholdMemberData, ThrowOnError>): RequestResult<HouseholdsRemoveHouseholdMemberResponses, HouseholdsRemoveHouseholdMemberErrors, ThrowOnError> => (options.client ?? client).delete<HouseholdsRemoveHouseholdMemberResponses, HouseholdsRemoveHouseholdMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/members/{user_id}',
    ...options
});

/**
 * Update Household Member
 *
 * Change the role of a household member.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * user_id: The ID of the user whose role changes.
 * member_in: The new role.
 *
 * Returns:
 * The updated membership.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * member is not in the household (404), or the change would leave the
 * household with no owner (400).
 */
export const householdsUpdateHouseholdMember = <ThrowOnError extends boolean = false>(options: Options<HouseholdsUpdateHouseholdMemberData, ThrowOnError>): RequestResult<HouseholdsUpdateHouseholdMemberResponses, HouseholdsUpdateHouseholdMemberErrors, ThrowOnError> => (options.client ?? client).patch<HouseholdsUpdateHouseholdMemberResponses, HouseholdsUpdateHouseholdMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/members/{user_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Household Invites
 *
 * List the invitations sent from this household.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context.
 * invite_status: An optional status to filter on.
 *
 * Returns:
 * The invitations, newest first.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const householdsListHouseholdInvites = <ThrowOnError extends boolean = false>(options?: Options<HouseholdsListHouseholdInvitesData, ThrowOnError>): RequestResult<HouseholdsListHouseholdInvitesResponses, HouseholdsListHouseholdInvitesErrors, ThrowOnError> => (options?.client ?? client).get<HouseholdsListHouseholdInvitesResponses, HouseholdsListHouseholdInvitesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/invites',
    ...options
});

/**
 * Create Household Invite
 *
 * Invite someone to share the household, and email them a link.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * invite_in: The address to invite and the role to give them.
 *
 * Returns:
 * The created invitation.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * household no longer exists (404), or that address already has an
 * invitation or is already a member (409).
 */
export const householdsCreateHouseholdInvite = <ThrowOnError extends boolean = false>(options: Options<HouseholdsCreateHouseholdInviteData, ThrowOnError>): RequestResult<HouseholdsCreateHouseholdInviteResponses, HouseholdsCreateHouseholdInviteErrors, ThrowOnError> => (options.client ?? client).post<HouseholdsCreateHouseholdInviteResponses, HouseholdsCreateHouseholdInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/invites',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke Household Invite
 *
 * Withdraw an invitation that has not been accepted.
 *
 * Args:
 * household_service: The household service dependency.
 * household: The current household context, which must be owned by the user.
 * invite_id: The ID of the invitation to withdraw.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the user is not an owner of the household (403), the
 * invitation does not exist in the household (404), or it was already
 * accepted or withdrawn (400).
 */
export const householdsRevokeHouseholdInvite = <ThrowOnError extends boolean = false>(options: Options<HouseholdsRevokeHouseholdInviteData, ThrowOnError>): RequestResult<HouseholdsRevokeHouseholdInviteResponses, HouseholdsRevokeHouseholdInviteErrors, ThrowOnError> => (options.client ?? client).delete<HouseholdsRevokeHouseholdInviteResponses, HouseholdsRevokeHouseholdInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/me/invites/{invite_id}',
    ...options
});

/**
 * Accept Household Invite
 *
 * Accept an invitation and join the household.
 *
 * The household created when you signed up is discarded if it is still
 * empty. If you have already recorded anything in it, joining is refused
 * rather than silently leaving that behind.
 *
 * Args:
 * household_service: The household service dependency.
 * category_service: The category service dependency.
 * current_user: The current authenticated user.
 * accept_in: The invitation token.
 *
 * Returns:
 * The household you joined.
 *
 * Raises:
 * HTTPException: If the token is not recognised (404), the invitation was
 * sent to a different address (403), it has expired or was already
 * used (400), or your current household holds data (409).
 */
export const householdsAcceptHouseholdInvite = <ThrowOnError extends boolean = false>(options: Options<HouseholdsAcceptHouseholdInviteData, ThrowOnError>): RequestResult<HouseholdsAcceptHouseholdInviteResponses, HouseholdsAcceptHouseholdInviteErrors, ThrowOnError> => (options.client ?? client).post<HouseholdsAcceptHouseholdInviteResponses, HouseholdsAcceptHouseholdInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/households/invites/accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Preview Household Invite
 *
 * Describe an invitation, for the page that offers to accept it.
 *
 * Public, because the recipient may not have an account yet. It carries only
 * what somebody needs in order to decide, and nothing about the household's
 * money.
 *
 * Args:
 * household_service: The household service dependency.
 * token: The invitation token.
 *
 * Returns:
 * The household name, who invited them, and when it expires.
 *
 * Raises:
 * HTTPException: If the token is not recognised (404), or the invitation
 * has expired or was already used (400).
 */
export const householdsPreviewHouseholdInvite = <ThrowOnError extends boolean = false>(options: Options<HouseholdsPreviewHouseholdInviteData, ThrowOnError>): RequestResult<HouseholdsPreviewHouseholdInviteResponses, HouseholdsPreviewHouseholdInviteErrors, ThrowOnError> => (options.client ?? client).get<HouseholdsPreviewHouseholdInviteResponses, HouseholdsPreviewHouseholdInviteErrors, ThrowOnError>({ url: '/api/v1/households/invites/{token}', ...options });

/**
 * List Categories
 *
 * List the categories of the household as a flat list.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived categories.
 * kind: An optional kind to filter on.
 * parent_id: An optional parent to filter on.
 *
 * Returns:
 * The categories.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const categoriesListCategories = <ThrowOnError extends boolean = false>(options?: Options<CategoriesListCategoriesData, ThrowOnError>): RequestResult<CategoriesListCategoriesResponses, CategoriesListCategoriesErrors, ThrowOnError> => (options?.client ?? client).get<CategoriesListCategoriesResponses, CategoriesListCategoriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/',
    ...options
});

/**
 * Create Category
 *
 * Create a category or subcategory.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_in: The category to create.
 *
 * Returns:
 * The created category.
 *
 * Raises:
 * HTTPException: If a sibling already has that name (409), or the parent is
 * missing (404) or invalid (400).
 */
export const categoriesCreateCategory = <ThrowOnError extends boolean = false>(options: Options<CategoriesCreateCategoryData, ThrowOnError>): RequestResult<CategoriesCreateCategoryResponses, CategoriesCreateCategoryErrors, ThrowOnError> => (options.client ?? client).post<CategoriesCreateCategoryResponses, CategoriesCreateCategoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Category Tree
 *
 * Get the categories of the household as a two level tree.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived categories.
 * kind: An optional kind to filter on.
 *
 * Returns:
 * The top level categories, each with its subcategories.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const categoriesGetCategoryTree = <ThrowOnError extends boolean = false>(options?: Options<CategoriesGetCategoryTreeData, ThrowOnError>): RequestResult<CategoriesGetCategoryTreeResponses, CategoriesGetCategoryTreeErrors, ThrowOnError> => (options?.client ?? client).get<CategoriesGetCategoryTreeResponses, CategoriesGetCategoryTreeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/tree',
    ...options
});

/**
 * Delete Category
 *
 * Delete a category that nothing references.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404), it
 * still has subcategories (409), or it is built in (400).
 */
export const categoriesDeleteCategory = <ThrowOnError extends boolean = false>(options: Options<CategoriesDeleteCategoryData, ThrowOnError>): RequestResult<CategoriesDeleteCategoryResponses, CategoriesDeleteCategoryErrors, ThrowOnError> => (options.client ?? client).delete<CategoriesDeleteCategoryResponses, CategoriesDeleteCategoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/{category_id}',
    ...options
});

/**
 * Get Category
 *
 * Get one category of the household.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category.
 *
 * Returns:
 * The category.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404).
 */
export const categoriesGetCategory = <ThrowOnError extends boolean = false>(options: Options<CategoriesGetCategoryData, ThrowOnError>): RequestResult<CategoriesGetCategoryResponses, CategoriesGetCategoryErrors, ThrowOnError> => (options.client ?? client).get<CategoriesGetCategoryResponses, CategoriesGetCategoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/{category_id}',
    ...options
});

/**
 * Update Category
 *
 * Rename, re-parent, archive or restore a category.
 *
 * Args:
 * category_service: The category service dependency.
 * household: The current household context.
 * category_id: The ID of the category to update.
 * category_in: The fields to update.
 *
 * Returns:
 * The updated category.
 *
 * Raises:
 * HTTPException: If the category or the new parent does not exist in the
 * household (404), a sibling already has the new name (409), or the
 * change is not allowed (400).
 */
export const categoriesUpdateCategory = <ThrowOnError extends boolean = false>(options: Options<CategoriesUpdateCategoryData, ThrowOnError>): RequestResult<CategoriesUpdateCategoryResponses, CategoriesUpdateCategoryErrors, ThrowOnError> => (options.client ?? client).patch<CategoriesUpdateCategoryResponses, CategoriesUpdateCategoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/categories/{category_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Accounts
 *
 * List the accounts of the household with their balances.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * include_archived: Whether to include archived accounts.
 * account_type: An optional type to filter on.
 * skip: Number of records to skip.
 * limit: Maximum number of records to return.
 *
 * Returns:
 * The accounts, and the total of the balances returned.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const accountsListAccounts = <ThrowOnError extends boolean = false>(options?: Options<AccountsListAccountsData, ThrowOnError>): RequestResult<AccountsListAccountsResponses, AccountsListAccountsErrors, ThrowOnError> => (options?.client ?? client).get<AccountsListAccountsResponses, AccountsListAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/',
    ...options
});

/**
 * Create Account
 *
 * Create an account.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_in: The account to create, with the balance it already held.
 *
 * Returns:
 * The created account.
 *
 * Raises:
 * HTTPException: If the household already has an account with that name
 * (409), or the household no longer exists (404).
 */
export const accountsCreateAccount = <ThrowOnError extends boolean = false>(options: Options<AccountsCreateAccountData, ThrowOnError>): RequestResult<AccountsCreateAccountResponses, AccountsCreateAccountErrors, ThrowOnError> => (options.client ?? client).post<AccountsCreateAccountResponses, AccountsCreateAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Account
 *
 * Delete an account that has no history.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404), or
 * it still has transactions (409).
 */
export const accountsDeleteAccount = <ThrowOnError extends boolean = false>(options: Options<AccountsDeleteAccountData, ThrowOnError>): RequestResult<AccountsDeleteAccountResponses, AccountsDeleteAccountErrors, ThrowOnError> => (options.client ?? client).delete<AccountsDeleteAccountResponses, AccountsDeleteAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/{account_id}',
    ...options
});

/**
 * Get Account
 *
 * Get one account of the household with its balance.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account.
 *
 * Returns:
 * The account.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404).
 */
export const accountsGetAccount = <ThrowOnError extends boolean = false>(options: Options<AccountsGetAccountData, ThrowOnError>): RequestResult<AccountsGetAccountResponses, AccountsGetAccountErrors, ThrowOnError> => (options.client ?? client).get<AccountsGetAccountResponses, AccountsGetAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/{account_id}',
    ...options
});

/**
 * Update Account
 *
 * Rename, re-type, archive or restore an account.
 *
 * The opening balance cannot be changed, since that would rewrite every
 * historical balance. Record an adjustment transaction instead.
 *
 * Args:
 * account_service: The account service dependency.
 * household: The current household context.
 * account_id: The ID of the account to update.
 * account_in: The fields to update.
 *
 * Returns:
 * The updated account.
 *
 * Raises:
 * HTTPException: If the account does not exist in the household (404), or
 * the household already has an account with the new name (409).
 */
export const accountsUpdateAccount = <ThrowOnError extends boolean = false>(options: Options<AccountsUpdateAccountData, ThrowOnError>): RequestResult<AccountsUpdateAccountResponses, AccountsUpdateAccountErrors, ThrowOnError> => (options.client ?? client).patch<AccountsUpdateAccountResponses, AccountsUpdateAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/{account_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Transactions
 *
 * List the transactions of the household.
 *
 * Any recurring transactions that have fallen due are recorded first, so the
 * ledger is up to date without needing a scheduler.
 *
 * Filtering by a parent category includes the spending filed under its
 * subcategories, unless include_subcategories is set to false.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * recurring_rule_service: The recurring rule service dependency, used to
 * bring the ledger up to date before reading it.
 * household: The current household context.
 * filters: The date range, account, category, kind, amount range, search
 * text, ordering and paging to apply.
 *
 * Returns:
 * The matching transactions and the total number of matches.
 *
 * Raises:
 * HTTPException: If the category filter names a category outside the
 * household (404), or a filter is not recognised (422).
 */
export const transactionsListTransactions = <ThrowOnError extends boolean = false>(options?: Options<TransactionsListTransactionsData, ThrowOnError>): RequestResult<TransactionsListTransactionsResponses, TransactionsListTransactionsErrors, ThrowOnError> => (options?.client ?? client).get<TransactionsListTransactionsResponses, TransactionsListTransactionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/transactions/',
    ...options
});

/**
 * Create Transaction
 *
 * Record an expense, some income, or a transfer between accounts.
 *
 * A transfer is an ordinary transaction with kind "transfer" and a
 * destination account, so it needs no separate endpoint.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_in: The transaction to record.
 *
 * Returns:
 * The recorded transaction.
 *
 * Raises:
 * HTTPException: If an account or category does not exist in the household
 * (404), or the transaction does not match its kind (400).
 */
export const transactionsCreateTransaction = <ThrowOnError extends boolean = false>(options: Options<TransactionsCreateTransactionData, ThrowOnError>): RequestResult<TransactionsCreateTransactionResponses, TransactionsCreateTransactionErrors, ThrowOnError> => (options.client ?? client).post<TransactionsCreateTransactionResponses, TransactionsCreateTransactionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/transactions/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Transaction
 *
 * Delete a transaction.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the transaction does not exist in the household (404).
 */
export const transactionsDeleteTransaction = <ThrowOnError extends boolean = false>(options: Options<TransactionsDeleteTransactionData, ThrowOnError>): RequestResult<TransactionsDeleteTransactionResponses, TransactionsDeleteTransactionErrors, ThrowOnError> => (options.client ?? client).delete<TransactionsDeleteTransactionResponses, TransactionsDeleteTransactionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/transactions/{transaction_id}',
    ...options
});

/**
 * Get Transaction
 *
 * Get one transaction of the household.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction.
 *
 * Returns:
 * The transaction.
 *
 * Raises:
 * HTTPException: If the transaction does not exist in the household (404).
 */
export const transactionsGetTransaction = <ThrowOnError extends boolean = false>(options: Options<TransactionsGetTransactionData, ThrowOnError>): RequestResult<TransactionsGetTransactionResponses, TransactionsGetTransactionErrors, ThrowOnError> => (options.client ?? client).get<TransactionsGetTransactionResponses, TransactionsGetTransactionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/transactions/{transaction_id}',
    ...options
});

/**
 * Update Transaction
 *
 * Edit a transaction.
 *
 * Args:
 * transaction_service: The transaction service dependency.
 * household: The current household context.
 * transaction_id: The ID of the transaction to edit.
 * transaction_in: The fields to change.
 *
 * Returns:
 * The updated transaction.
 *
 * Raises:
 * HTTPException: If the transaction, an account or a category does not
 * exist in the household (404), or the change does not match the kind
 * (400).
 */
export const transactionsUpdateTransaction = <ThrowOnError extends boolean = false>(options: Options<TransactionsUpdateTransactionData, ThrowOnError>): RequestResult<TransactionsUpdateTransactionResponses, TransactionsUpdateTransactionErrors, ThrowOnError> => (options.client ?? client).patch<TransactionsUpdateTransactionResponses, TransactionsUpdateTransactionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/transactions/{transaction_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Budgets
 *
 * List the budgets of the household for one month.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * The budgets, and the total of the limits.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const budgetsListBudgets = <ThrowOnError extends boolean = false>(options: Options<BudgetsListBudgetsData, ThrowOnError>): RequestResult<BudgetsListBudgetsResponses, BudgetsListBudgetsErrors, ThrowOnError> => (options.client ?? client).get<BudgetsListBudgetsResponses, BudgetsListBudgetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/',
    ...options
});

/**
 * Create Budget
 *
 * Set the spending limit of a category for a month.
 *
 * A limit on a parent category covers the spending filed under its
 * subcategories, so a parent and one of its children cannot both be
 * budgeted in the same month.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_in: The category, month and limit.
 *
 * Returns:
 * The created budget.
 *
 * Raises:
 * HTTPException: If the category does not exist in the household (404),
 * the category or an overlapping one is already budgeted that month
 * (409), or the category is an income category (400).
 */
export const budgetsCreateBudget = <ThrowOnError extends boolean = false>(options: Options<BudgetsCreateBudgetData, ThrowOnError>): RequestResult<BudgetsCreateBudgetResponses, BudgetsCreateBudgetErrors, ThrowOnError> => (options.client ?? client).post<BudgetsCreateBudgetResponses, BudgetsCreateBudgetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Bulk Upsert Budgets
 *
 * Replace the whole set of budgets for one month.
 *
 * This is what a month-at-a-time editing screen saves, so it is one request
 * rather than one per category. A category left out of the set has its limit
 * removed.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * bulk_in: The month and the complete set of limits.
 *
 * Returns:
 * The budgets now set for that month.
 *
 * Raises:
 * HTTPException: If a category does not exist in the household (404), the
 * set budgets both a parent and its subcategory (409), or it names a
 * category twice or an income category (400).
 */
export const budgetsBulkUpsertBudgets = <ThrowOnError extends boolean = false>(options: Options<BudgetsBulkUpsertBudgetsData, ThrowOnError>): RequestResult<BudgetsBulkUpsertBudgetsResponses, BudgetsBulkUpsertBudgetsErrors, ThrowOnError> => (options.client ?? client).put<BudgetsBulkUpsertBudgetsResponses, BudgetsBulkUpsertBudgetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Copy Budgets
 *
 * Copy the budgets of one month onto another.
 *
 * Budgets do not roll over, so this is what makes "same as last month" a
 * single action rather than retyping every limit. Overwriting replaces the
 * target month with the source: a limit the source does not set is removed.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * copy_in: The source month, the target month, and whether to replace
 * limits already set on the target.
 *
 * Returns:
 * The budgets now set for the target month.
 *
 * Raises:
 * HTTPException: If the target month already has budgets and overwrite was
 * not requested, or the source month budgets both a parent and its
 * subcategory (409).
 */
export const budgetsCopyBudgets = <ThrowOnError extends boolean = false>(options: Options<BudgetsCopyBudgetsData, ThrowOnError>): RequestResult<BudgetsCopyBudgetsResponses, BudgetsCopyBudgetsErrors, ThrowOnError> => (options.client ?? client).post<BudgetsCopyBudgetsResponses, BudgetsCopyBudgetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/copy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Budget
 *
 * Remove a budget.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget to remove.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsDeleteBudget = <ThrowOnError extends boolean = false>(options: Options<BudgetsDeleteBudgetData, ThrowOnError>): RequestResult<BudgetsDeleteBudgetResponses, BudgetsDeleteBudgetErrors, ThrowOnError> => (options.client ?? client).delete<BudgetsDeleteBudgetResponses, BudgetsDeleteBudgetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/{budget_id}',
    ...options
});

/**
 * Get Budget
 *
 * Get one budget of the household.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget.
 *
 * Returns:
 * The budget.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsGetBudget = <ThrowOnError extends boolean = false>(options: Options<BudgetsGetBudgetData, ThrowOnError>): RequestResult<BudgetsGetBudgetResponses, BudgetsGetBudgetErrors, ThrowOnError> => (options.client ?? client).get<BudgetsGetBudgetResponses, BudgetsGetBudgetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/{budget_id}',
    ...options
});

/**
 * Update Budget
 *
 * Change the limit of a budget.
 *
 * The category and month identify the budget, so changing either means
 * setting a different budget rather than editing this one.
 *
 * Args:
 * budget_service: The budget service dependency.
 * household: The current household context.
 * budget_id: The ID of the budget to change.
 * budget_in: The new limit.
 *
 * Returns:
 * The updated budget.
 *
 * Raises:
 * HTTPException: If the budget does not exist in the household (404).
 */
export const budgetsUpdateBudget = <ThrowOnError extends boolean = false>(options: Options<BudgetsUpdateBudgetData, ThrowOnError>): RequestResult<BudgetsUpdateBudgetResponses, BudgetsUpdateBudgetErrors, ThrowOnError> => (options.client ?? client).patch<BudgetsUpdateBudgetResponses, BudgetsUpdateBudgetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/budgets/{budget_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Recurring Rules
 *
 * List the recurring rules of the household.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * is_active: An optional active state to filter on.
 * skip: Number of records to skip.
 * limit: Maximum number of records to return.
 *
 * Returns:
 * The rules, soonest due first.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const recurringRulesListRecurringRules = <ThrowOnError extends boolean = false>(options?: Options<RecurringRulesListRecurringRulesData, ThrowOnError>): RequestResult<RecurringRulesListRecurringRulesResponses, RecurringRulesListRecurringRulesErrors, ThrowOnError> => (options?.client ?? client).get<RecurringRulesListRecurringRulesResponses, RecurringRulesListRecurringRulesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/',
    ...options
});

/**
 * Create Recurring Rule
 *
 * Create a recurring rule, such as rent or a subscription.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_in: The rule to create.
 *
 * Returns:
 * The created rule, with the date it first falls due.
 *
 * Raises:
 * HTTPException: If an account or category does not exist in the household
 * (404), or the rule does not match its kind or would never come due
 * (400).
 */
export const recurringRulesCreateRecurringRule = <ThrowOnError extends boolean = false>(options: Options<RecurringRulesCreateRecurringRuleData, ThrowOnError>): RequestResult<RecurringRulesCreateRecurringRuleResponses, RecurringRulesCreateRecurringRuleErrors, ThrowOnError> => (options.client ?? client).post<RecurringRulesCreateRecurringRuleResponses, RecurringRulesCreateRecurringRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Upcoming Occurrences
 *
 * Show the recurring occurrences that have not been recorded yet.
 *
 * Read only: nothing is created, so this can be used to plan ahead.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * until: The last day to look ahead to, inclusive. Defaults to two months out.
 *
 * Returns:
 * The projected occurrences, soonest first, and what they add up to.
 *
 * Raises:
 * HTTPException: If the user belongs to no household (404).
 */
export const recurringRulesListUpcomingOccurrences = <ThrowOnError extends boolean = false>(options?: Options<RecurringRulesListUpcomingOccurrencesData, ThrowOnError>): RequestResult<RecurringRulesListUpcomingOccurrencesResponses, RecurringRulesListUpcomingOccurrencesErrors, ThrowOnError> => (options?.client ?? client).get<RecurringRulesListUpcomingOccurrencesResponses, RecurringRulesListUpcomingOccurrencesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/upcoming',
    ...options
});

/**
 * Run Recurring Rules
 *
 * Record the transactions the rules have fallen due for.
 *
 * The read paths do this automatically, so this endpoint exists for catching
 * up explicitly, or for creating occurrences ahead of today.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * until: The last day to record up to, inclusive. Defaults to today.
 *
 * Returns:
 * How many transactions were created and how many rules moved on.
 *
 * Raises:
 * HTTPException: If a rule points at an account that no longer exists (404).
 */
export const recurringRulesRunRecurringRules = <ThrowOnError extends boolean = false>(options?: Options<RecurringRulesRunRecurringRulesData, ThrowOnError>): RequestResult<RecurringRulesRunRecurringRulesResponses, RecurringRulesRunRecurringRulesErrors, ThrowOnError> => (options?.client ?? client).post<RecurringRulesRunRecurringRulesResponses, RecurringRulesRunRecurringRulesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/run',
    ...options
});

/**
 * Delete Recurring Rule
 *
 * Delete a recurring rule.
 *
 * The transactions it already created are kept: they happened, and the rule
 * is only the template that made them.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule to delete.
 *
 * Returns:
 * A confirmation message.
 *
 * Raises:
 * HTTPException: If the rule does not exist in the household (404).
 */
export const recurringRulesDeleteRecurringRule = <ThrowOnError extends boolean = false>(options: Options<RecurringRulesDeleteRecurringRuleData, ThrowOnError>): RequestResult<RecurringRulesDeleteRecurringRuleResponses, RecurringRulesDeleteRecurringRuleErrors, ThrowOnError> => (options.client ?? client).delete<RecurringRulesDeleteRecurringRuleResponses, RecurringRulesDeleteRecurringRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/{rule_id}',
    ...options
});

/**
 * Get Recurring Rule
 *
 * Get one recurring rule of the household.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule.
 *
 * Returns:
 * The rule.
 *
 * Raises:
 * HTTPException: If the rule does not exist in the household (404).
 */
export const recurringRulesGetRecurringRule = <ThrowOnError extends boolean = false>(options: Options<RecurringRulesGetRecurringRuleData, ThrowOnError>): RequestResult<RecurringRulesGetRecurringRuleResponses, RecurringRulesGetRecurringRuleErrors, ThrowOnError> => (options.client ?? client).get<RecurringRulesGetRecurringRuleResponses, RecurringRulesGetRecurringRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/{rule_id}',
    ...options
});

/**
 * Update Recurring Rule
 *
 * Edit a recurring rule, or pause it by setting is_active to false.
 *
 * Args:
 * recurring_rule_service: The recurring rule service dependency.
 * household: The current household context.
 * rule_id: The ID of the rule to edit.
 * rule_in: The fields to change.
 *
 * Returns:
 * The updated rule.
 *
 * Raises:
 * HTTPException: If the rule or the category does not exist in the
 * household (404), or the new schedule would never come due (400).
 */
export const recurringRulesUpdateRecurringRule = <ThrowOnError extends boolean = false>(options: Options<RecurringRulesUpdateRecurringRuleData, ThrowOnError>): RequestResult<RecurringRulesUpdateRecurringRuleResponses, RecurringRulesUpdateRecurringRuleErrors, ThrowOnError> => (options.client ?? client).patch<RecurringRulesUpdateRecurringRuleResponses, RecurringRulesUpdateRecurringRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recurring-rules/{rule_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Spend By Category
 *
 * Break a month's spending down by category.
 *
 * Transfers can never appear here: they are their own kind, so moving money
 * between your own accounts is not spending.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 * kind: Whether to break down expenses or income.
 * depth: Whether to report per subcategory, or rolled up to the parent.
 *
 * Returns:
 * One slice per category, largest first, with each share of the total.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsSpendByCategory = <ThrowOnError extends boolean = false>(options: Options<ReportsSpendByCategoryData, ThrowOnError>): RequestResult<ReportsSpendByCategoryResponses, ReportsSpendByCategoryErrors, ThrowOnError> => (options.client ?? client).get<ReportsSpendByCategoryResponses, ReportsSpendByCategoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reports/spend-by-category',
    ...options
});

/**
 * Spend Over Time
 *
 * Chart spending over a period.
 *
 * Buckets with no activity are returned as zero rather than left out, so the
 * chart cannot imply a gap in time that is not there.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * date_from: First day of the period, inclusive.
 * date_to: Last day of the period, inclusive.
 * granularity: Whether to bucket by day or by month.
 * kind: Whether to chart expenses or income.
 * account_id: An optional account to restrict to.
 * category_id: An optional category to restrict to. A parent category
 * includes the spending filed under its subcategories.
 *
 * Returns:
 * One point per bucket, oldest first.
 *
 * Raises:
 * HTTPException: If the range starts after it ends (400), the range is too
 * large for daily buckets (422), or the category does not exist in the
 * household (404).
 */
export const reportsSpendOverTime = <ThrowOnError extends boolean = false>(options: Options<ReportsSpendOverTimeData, ThrowOnError>): RequestResult<ReportsSpendOverTimeResponses, ReportsSpendOverTimeErrors, ThrowOnError> => (options.client ?? client).get<ReportsSpendOverTimeResponses, ReportsSpendOverTimeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reports/spend-over-time',
    ...options
});

/**
 * Income Expense
 *
 * Compare money in against money out, month by month.
 *
 * Each month also carries the running net, which is the savings trend.
 * Transfers appear on neither side.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month_from: First month of the range, in "YYYY-MM" form.
 * month_to: Last month of the range, in "YYYY-MM" form.
 *
 * Returns:
 * One entry per month, oldest first, plus the totals for the range.
 *
 * Raises:
 * HTTPException: If the range starts after it ends (400), or covers more
 * than ten years (422).
 */
export const reportsIncomeExpense = <ThrowOnError extends boolean = false>(options: Options<ReportsIncomeExpenseData, ThrowOnError>): RequestResult<ReportsIncomeExpenseResponses, ReportsIncomeExpenseErrors, ThrowOnError> => (options.client ?? client).get<ReportsIncomeExpenseResponses, ReportsIncomeExpenseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reports/income-expense',
    ...options
});

/**
 * Budget Progress
 *
 * Compare a month's spending against the limits set for it.
 *
 * A limit on a parent category counts the spending filed under its
 * subcategories, so budgeting "Food & Drink" tracks what was spent on
 * groceries and restaurants together.
 *
 * Args:
 * report_service: The report service dependency.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * One row per budget, sorted by how much of each limit is used, plus the
 * spending that had no limit at all.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsBudgetProgress = <ThrowOnError extends boolean = false>(options: Options<ReportsBudgetProgressData, ThrowOnError>): RequestResult<ReportsBudgetProgressResponses, ReportsBudgetProgressErrors, ThrowOnError> => (options.client ?? client).get<ReportsBudgetProgressResponses, ReportsBudgetProgressErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reports/budget-progress',
    ...options
});

/**
 * Month Summary
 *
 * Gather the dashboard figures for one month.
 *
 * One request rather than several, so opening the app is a single round trip.
 * Any recurring transactions that have fallen due are recorded first, so the
 * dashboard is not out of date the moment it loads.
 *
 * Args:
 * report_service: The report service dependency.
 * recurring_rule_service: The recurring rule service dependency, used to
 * bring the ledger up to date before reading it.
 * household: The current household context.
 * month: The month, in "YYYY-MM" form.
 *
 * Returns:
 * The month's income, spending and net, the current net worth, how much
 * was budgeted, how many categories went over, and the biggest categories.
 *
 * Raises:
 * HTTPException: If the month is not in "YYYY-MM" form (422).
 */
export const reportsMonthSummary = <ThrowOnError extends boolean = false>(options: Options<ReportsMonthSummaryData, ThrowOnError>): RequestResult<ReportsMonthSummaryResponses, ReportsMonthSummaryErrors, ThrowOnError> => (options.client ?? client).get<ReportsMonthSummaryResponses, ReportsMonthSummaryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reports/summary',
    ...options
});

/**
 * Login Access Token
 *
 * Get an access token for future requests.
 *
 * The obtained token could be used as a Bearer token in subsequent authenticated requests.
 *
 * Args:
 * user_service: The user service dependency.
 * email_verification_service: The email verification service dependency.
 * form_data: The OAuth2 password request form data.
 *
 * Returns:
 * A token containing the access token.
 *
 * Raises:
 * HTTPException: If the credentials do not check out (401), or the user is inactive (403). The 401
 * is deliberately the same whether or not the address is registered, so that the endpoint cannot
 * be used to enumerate accounts. For 403, the error message will indicate if email verification
 * is pending.
 */
export const loginLoginAccessToken = <ThrowOnError extends boolean = false>(options: Options<LoginLoginAccessTokenData, ThrowOnError>): RequestResult<LoginLoginAccessTokenResponses, LoginLoginAccessTokenErrors, ThrowOnError> => (options.client ?? client).post<LoginLoginAccessTokenResponses, LoginLoginAccessTokenErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/v1/login/access-token',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Request Password Reset
 *
 * Request a password reset.
 *
 * For security reasons, this always returns success even if the email doesn't exist.
 * This prevents user enumeration attacks.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * user_service: The user service dependency.
 * password_reset_in: The password reset request payload.
 *
 * Returns:
 * A message indicating that the request was successful.
 */
export const passwordResetRequestPasswordReset = <ThrowOnError extends boolean = false>(options: Options<PasswordResetRequestPasswordResetData, ThrowOnError>): RequestResult<PasswordResetRequestPasswordResetResponses, PasswordResetRequestPasswordResetErrors, ThrowOnError> => (options.client ?? client).post<PasswordResetRequestPasswordResetResponses, PasswordResetRequestPasswordResetErrors, ThrowOnError>({
    url: '/api/v1/password-reset/request',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Verify Password Reset Token
 *
 * Verify a password reset token is valid.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * password_reset_verify: The password reset verification payload.
 *
 * Returns:
 * A message indicating that the token is valid.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), used (400), or not found (404).
 */
export const passwordResetVerifyPasswordResetToken = <ThrowOnError extends boolean = false>(options: Options<PasswordResetVerifyPasswordResetTokenData, ThrowOnError>): RequestResult<PasswordResetVerifyPasswordResetTokenResponses, PasswordResetVerifyPasswordResetTokenErrors, ThrowOnError> => (options.client ?? client).post<PasswordResetVerifyPasswordResetTokenResponses, PasswordResetVerifyPasswordResetTokenErrors, ThrowOnError>({
    url: '/api/v1/password-reset/verify',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm Password Reset
 *
 * Reset a user's password.
 *
 * Args:
 * password_reset_service: The password reset service dependency.
 * user_service: The user service dependency.
 * password_reset_confirm: The password reset confirmation payload.
 *
 * Returns:
 * A message indicating that the password was reset successfully.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), used (400), or not found (404).
 */
export const passwordResetConfirmPasswordReset = <ThrowOnError extends boolean = false>(options: Options<PasswordResetConfirmPasswordResetData, ThrowOnError>): RequestResult<PasswordResetConfirmPasswordResetResponses, PasswordResetConfirmPasswordResetErrors, ThrowOnError> => (options.client ?? client).post<PasswordResetConfirmPasswordResetResponses, PasswordResetConfirmPasswordResetErrors, ThrowOnError>({
    url: '/api/v1/password-reset/confirm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resend Verification Email
 *
 * Send or resend an email verification.
 *
 * For security reasons, this always returns success even if the email doesn't exist.
 * This prevents user enumeration attacks.
 *
 * Args:
 * email_verification_service: The email verification service dependency.
 * user_service: The user service dependency.
 * email_verification_request: The email verification request payload.
 *
 * Returns:
 * A message indicating that the request was successful.
 */
export const emailVerificationResendVerificationEmail = <ThrowOnError extends boolean = false>(options: Options<EmailVerificationResendVerificationEmailData, ThrowOnError>): RequestResult<EmailVerificationResendVerificationEmailResponses, EmailVerificationResendVerificationEmailErrors, ThrowOnError> => (options.client ?? client).post<EmailVerificationResendVerificationEmailResponses, EmailVerificationResendVerificationEmailErrors, ThrowOnError>({
    url: '/api/v1/email-verification/send',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Verify Email
 *
 * Verify a user's email address.
 *
 * Args:
 * email_verification_service: The email verification service dependency.
 * user_service: The user service dependency.
 * email_verification_confirm: The email verification confirmation payload.
 *
 * Returns:
 * A message indicating that the email was verified successfully.
 *
 * Raises:
 * HTTPException: If the token is invalid (400), expired (400), already used (400), or not found (404).
 */
export const emailVerificationVerifyEmail = <ThrowOnError extends boolean = false>(options: Options<EmailVerificationVerifyEmailData, ThrowOnError>): RequestResult<EmailVerificationVerifyEmailResponses, EmailVerificationVerifyEmailErrors, ThrowOnError> => (options.client ?? client).post<EmailVerificationVerifyEmailResponses, EmailVerificationVerifyEmailErrors, ThrowOnError>({
    url: '/api/v1/email-verification/verify',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
